<?xml version="1.0" encoding="utf-8"?>
<search>
  
  
    
    <entry>
      <title><![CDATA[打造自己的终端和命令行item2和zsh]]></title>
      <url>http://upeng.github.io/2016/12/04/zsh-and-item2/</url>
      <content type="html"><![CDATA[<p>关于zsh和item2的关系可以理解为，zsh就是linux下bash shell的升级版；iterm2是termainal的升级版，可以自定义terminal主题配色；<code>cmd + ,</code>呼出配置。可以新增配置，并将之设置默认，（默认配置前有个⭐）<br><a id="more"></a></p>
<h3 id="zsh的高效命令（常用）"><a href="#zsh的高效命令（常用）" class="headerlink" title="zsh的高效命令（常用）"></a>zsh的高效命令（常用）</h3><ol>
<li><p>zsh 的命令<strong>补全</strong>功能非常强大，可以补齐路径，补齐命令，补齐参数等。你记住 tar 的所有参数了吗 ，只要 tar -加 tab 键</p>
</li>
<li><p>按下 tab 键显示出所有待选项后，再按一次 tab 键，即进入选择模式.进入选择模式后，按 tab 切向下一个选项，按 <code>shift+tab</code> 键切向上一个选项，<code>ctrl+f/b/n/p</code>可以向前后左右切换</p>
</li>
<li><p><code>ps aux | grep xxx</code>查进程的 PID，然后kill PID有了 zsh，直接kill xxx然后按下 tab 键即可</p>
<pre><code>kill emac
# 按下 tab，变成：
kill 59683
</code></pre></li>
<li><p>zsh的历史记录跨 session，可以共享。历史记录支持受限查找。比如，输入git，再按向上箭头，会搜索用过的所有 git 命令</p>
</li>
<li><p>zsh 的目录跳转更为智能，你无需输入cd，直接输入路径即可。<code>..</code>表示后退一级目录，<code>../../</code>表示后退两级，依次类推。（<code>...</code>的作用和<code>../../</code>相同）</p>
</li>
<li><p>输入d回车，将列出当前 session 访问过的所有目录，再输入提示的数字即可进入相应目录。</p>
</li>
</ol>
<h3 id="zsh配置"><a href="#zsh配置" class="headerlink" title="zsh配置"></a>zsh配置</h3><p>位于 ~/.zshrc，可以修改theme,当前使用cloud,可以自定义alias</p>
<h3 id="iterm2的配置"><a href="#iterm2的配置" class="headerlink" title="iterm2的配置"></a>iterm2的配置</h3><p>color配色方案：colors import我们githubs主题即可，更多关于iterm的信息见<a href="https://item2.com。" target="_blank" rel="external">https://item2.com。</a><br>比如选择dracula配色方案<a href="https://github.com/dracula/iterm" target="_blank" rel="external">https://github.com/dracula/iterm</a> </p>
<blockquote>
<p><code>working directory：Reuse previous&#39;directory</code> //directory总是展示当前dirname</p>
</blockquote>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[php常用的命令行]]></title>
      <url>http://upeng.github.io/2016/11/13/php-command/</url>
      <content type="html"><![CDATA[<blockquote>
<p>经常使用到的命令行</p>
</blockquote>
<ul>
<li>查看php文件是否有语法错误<br><code>php -ln filename.php</code></li>
</ul>
<a id="more"></a>
<ul>
<li><p>执行php文件<br><code>php filename.php</code><br><code>php -f filename.php</code></p>
</li>
<li><p>搜索php.ini关键词信息<br><code>php -i | grep keyword</code></p>
</li>
<li><p>查询php.ini位置信息<br><code>php --ini</code></p>
</li>
<li><p>查看php扩展模块<br><code>php -m</code></p>
</li>
<li><p>查看php版本<br><code>php -v</code></p>
</li>
</ul>
<p>/<strong>待补充</strong>/</p>
<blockquote>
<p>上述<code>查询php.ini</code>和<code>检测语法错误</code>很好用，源于查看session配置和运行脚本没有任何输出。<br>先学会命令行，再用IDE吧，啥？VIM~</p>
</blockquote>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[静态方法和静态成员变量]]></title>
      <url>http://upeng.github.io/2016/10/12/static/</url>
      <content type="html"><![CDATA[<blockquote>
<p>问题来源：</p>
</blockquote>
<p>写了一个<code>Model_User</code>，创建了2个静态方法func1和func2，2个静态方法中都含有公共的连接数据库的逻辑，突然想到代码重用，然后创建了构造方法，将连接数据库的逻辑放在<code>__construct()</code>中，然后去调用<code>Model_User</code>的静态方法<br><a id="more"></a></p>
<pre><code>Model_User::func1();
</code></pre><p><code>报错：</code></p>
<pre><code>Creating default object from empty value。。。
Using $this when not in object context in。。。。
</code></pre><p>因为我们直接使用类调用静态方法的方式，是根本不会走到构造方法的，而构造方法只有在new实例的时候才会调用。同时因为<code>$this</code>本身就是一个instance（实例）而我们根本就没有new 对象，所以。。。<br><strong>注意</strong></p>
<ol>
<li>如果使用类调用静态方法的形式，那么要注意<code>__construct</code>里面的操作就over了，因为<code>__construct</code>不会调用；</li>
<li>不能在静态方法中使用<code>$this</code>去调用普通成员变量和成员函数，因为你没有实例化对象，而$this就是一个instance;就算这样也是不可行的<pre><code>$obj = new Model_User
$obj-&gt;func1();
</code></pre><blockquote>
<p>正常情况下，静态方法中是不能调用非静态成员变量或非静态方法的！</p>
</blockquote>
</li>
</ol>
<p>静态成员变量和静态方法存储在堆中(一个公共的池子)。<br>所有实例化的对象instance都可以使用和操作它。<br>举例子：</p>
<pre><code>class Apple
{
    public static $a = 1;
    public $b = 1;

    public function set(2)
    {
        self::$a = $num; 
        $this-&gt;b = $num;
    }

    public function get()
    {
        return array(self::$a, $this-&gt;b);
    }
}

$apple1 = new Apple();
$apple1-&gt;set(2);
$res1 = $apple1-&gt;get();

$apple2 = new Apple();
$res2 = $apple2-&gt;get();

print_r($res1);  //array(2,2)
print_r($res2);  //array(2,1)
</code></pre><p>很明显，静态成员变量和普通成员变量的区别；在实际应用场景中，需要合理使用</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[从编写到发布laravel扩展]]></title>
      <url>http://upeng.github.io/2016/10/11/publish-laravel-package/</url>
      <content type="html"><![CDATA[<h3 id="工具准备"><a href="#工具准备" class="headerlink" title="工具准备"></a>工具准备</h3><blockquote>
<p>创建开源项目三工具github、packageist、composer</p>
</blockquote>
<ul>
<li>github: git仓库，是用于存放源代码的地方</li>
<li>packagist: 用于发布github上的扩展程序或者项目</li>
<li>composer: 用于使用代码的工具</li>
</ul>
<a id="more"></a>
<p><code>github-&gt;packagist-&gt;composer</code></p>
<ol>
<li>怎样写一个laravel扩展，发布到packagist，提供composer方法</li>
<li>git命令熟悉</li>
</ol>
<h3 id="写laravel扩展"><a href="#写laravel扩展" class="headerlink" title="写laravel扩展"></a>写laravel扩展</h3><h4 id="本地编写"><a href="#本地编写" class="headerlink" title="本地编写"></a>本地编写</h4><p>本地编写package，完成后，在laravel根目录下的composer.json中添加一行<br><code>&quot;Upstriving\\Curd\\&quot;: &quot;package/upstriving/curd/src&quot;</code></p>
<pre><code> &quot;autoload&quot;: {
        &quot;classmap&quot;: [
            &quot;database&quot;
        ],
        &quot;psr-4&quot;: {
            &quot;App\\&quot;: &quot;app/&quot;,
            &quot;Upstriving\\Curd\\&quot;: &quot;package/upstriving/curd/src&quot;
        }
    },
</code></pre><p>添加之后<code>composer dump-autoload</code>将之加到路由中<br><code>php artisan tinker</code>测试</p>
<pre><code>&gt;&gt;&gt; app(&#39;sqladmin&#39;)
=&gt; Upstriving\Curd\SQLAdmin {#629}
&gt;&gt;&gt; app(&#39;sqladmin&#39;)-&gt;makeSelect(&#39;user&#39;,&#39;id,name&#39;,[&#39;id&#39;=&gt;2]);
=&gt; [
     &quot;sql&quot; =&gt; &quot;select id,name from user where id=?&quot;,
     &quot;data&quot; =&gt; [
       2,
     ],
   ]
&gt;&gt;&gt; app(&#39;sqladmin&#39;)-&gt;makeDelete(&#39;user&#39;,[&#39;name&#39;=&gt;&#39;upfriend&#39;])
=&gt; [
     &quot;sql&quot; =&gt; &quot;delete from user where `name`=?&quot;,
     &quot;data&quot; =&gt; [
       &quot;upfriend&quot;,
     ],
   ]
</code></pre><h4 id="生成composer-json"><a href="#生成composer-json" class="headerlink" title="生成composer.json"></a>生成composer.json</h4><pre><code>composer init
This command will guide you through creating your composer.json config.

Package name (&lt;vendor&gt;/&lt;name&gt;) [yupeng6/curd]: upstriving/curd
Description []: assemble sql statements quickly
Author [upeng &lt;825415473@qq.com&gt;, n to skip]:
Minimum Stability []: dev
Package Type (e.g. library, project, metapackage, composer-plugin) []:
License []: MIT

Define your dependencies.

Would you like to define your dependencies (require) interactively [yes]? no
Would you like to define your dev dependencies (require-dev) interactively [yes]? no

{
    &quot;name&quot;: &quot;upstriving/curd&quot;,
    &quot;description&quot;: &quot;assemble sql statements quickly&quot;,
    &quot;license&quot;: &quot;MIT&quot;,
    &quot;authors&quot;: [
        {
            &quot;name&quot;: &quot;upeng&quot;,
            &quot;email&quot;: &quot;825415473@qq.com&quot;
        }
    ],
    &quot;minimum-stability&quot;: &quot;dev&quot;,
    &quot;require&quot;: {}
}

Do you confirm generation [yes]? yes
</code></pre><h4 id="生成composer-json-1"><a href="#生成composer-json-1" class="headerlink" title="生成composer.json"></a>生成composer.json</h4><blockquote>
<p>添加autoload</p>
<pre><code>&quot;autoload&quot;: {
    &quot;psr-4&quot;: {
         &quot;Upstriving\\Curd\\&quot;: &quot;src/&quot;
    }
}
</code></pre></blockquote>
<h4 id="推送到github"><a href="#推送到github" class="headerlink" title="推送到github"></a>推送到github</h4><blockquote>
<p>当前扩展目录下执行</p>
</blockquote>
<pre><code>git init
git add .
git commit -m &#39;add new package&#39;
</code></pre><p>github手动创建一个<code>repository(laravel-sqladmin)</code><br>在本地目录下，复制两行命令并执行，将本地代码推送到github</p>
<pre><code>git remote add origin git@github.com:upeng/laravel-sqladmin.git
git push -u origin master
</code></pre><p>新增readme.md</p>
<h4 id="通过github的package服务发布到packagist中"><a href="#通过github的package服务发布到packagist中" class="headerlink" title="通过github的package服务发布到packagist中"></a>通过github的package服务发布到packagist中</h4><p>供其他开发者通过composer使用<br>登录<code>https://packagist.org/</code>,<br><img src="http://ww2.sinaimg.cn/mw1024/006iFiRLjw1f8o4ehp6l9j31c30kejve.jpg" alt="此处输入图片的描述"><br>拿到token值，回到<code>github/larvavel-sqladmin/settings/integrations &amp; service</code><br>执行<code>add service/packagist</code><br><img src="http://ww2.sinaimg.cn/mw1024/006iFiRLjw1f8o4eibz58j31j50u9gsy.jpg" alt="此处输入图片的描述"><br>输入用户名user和token(从packagist中获取)<br>成功之后如下<br><img src="http://ww3.sinaimg.cn/mw1024/006iFiRLjw1f8o4eizjdvj31k70ra7bl.jpg" alt="此处输入图片的描述"></p>
<p>再次回到packagist中，submit刚才那个repository地址<br><img src="http://ww3.sinaimg.cn/mw1024/006iFiRLjw1f8o4ejjp7zj30wk0d8jsx.jpg" alt="此处输入图片的描述"><br><img src="http://ww2.sinaimg.cn/mw1024/006iFiRLjw1f8o4ek6aagj30wp0epmzr.jpg" alt="此处输入图片的描述"><br><img src="http://ww1.sinaimg.cn/mw1024/006iFiRLjw1f8o4ektd7qj318o0hg77p.jpg" alt="此处输入图片的描述"></p>
<p>提示这个package is not auto_updated，需要到github中设置一下<br>github/larvavel-sqladmin/settings/integrations &amp; service/packagist<br>点击test service，<br><img src="http://ww3.sinaimg.cn/mw1024/006iFiRLjw1f8o4elc6eej31600q6wiv.jpg" alt="此处输入图片的描述"></p>
<p>如果显示<br><code>Okay, the test payload is on its way.</code><br>说明自动更新没有问题，再次回到packagist发现刚刚的提示已经不见了<br><img src="http://ww3.sinaimg.cn/mw1024/006iFiRLjw1f8o4em26svj30n10d5myx.jpg" alt="此处输入图片的描述"></p>
<h4 id="修改或新增文件，观察是否实时更新到packagist"><a href="#修改或新增文件，观察是否实时更新到packagist" class="headerlink" title="修改或新增文件，观察是否实时更新到packagist"></a>修改或新增文件，观察是否实时更新到packagist</h4><blockquote>
<p>新增readme.file</p>
<pre><code>git add .
git commit -m &#39;&#39;
git push
</code></pre><p>增加版本号</p>
</blockquote>
<p><code>git tag 1.0 -a</code><br>输入: release version 1.0<br><code>git push --tags</code><br>发现github上多了一个版本号,packagist版本也随之更新<br><img src="http://ww3.sinaimg.cn/mw1024/006iFiRLjw1f8o4emqsblj312x0nv446.jpg" alt="此处输入图片的描述"></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[mysql主从复制原理理解与实践]]></title>
      <url>http://upeng.github.io/2016/09/12/mysql-master-slave/</url>
      <content type="html"><![CDATA[<p>只从最简单的mysql主从复制原理与实践着手，之前看到Yshiba老师讲解的挺细致透彻，顺便做下笔记并实践一下。</p>
<h2 id="主从复制原理-replication"><a href="#主从复制原理-replication" class="headerlink" title="主从复制原理 replication"></a>主从复制原理 replication</h2><p>主服务器(master)的binlog,记录master的各种操作（insert、update、delete），master的磁盘只要发生变化就会记录到自己的binlog里面。</p>
<p>从服务器（slave）如果能读取到master的binlog,那么就可以将master的所做操作跟着执行一遍。但是由于master的binlog中含有master的一些信息，所以需要对binlog进行分析加工，slave的relaylog就是干这种事的，实际上slave将使用relaylog终极日志上的sql操作，保证主从数据一致性。<br><a id="more"></a><br><img src="https://raw.githubusercontent.com/upeng/upeng.github.io/master/image/mysql-master-slave.gif" alt="mysql-master-slave"></p>
<p><strong>总结</strong>：</p>
<ol>
<li>master要配置binlog</li>
<li>slave要配置relaylog</li>
<li>master需要grant slave账号使得slave有权读取master的binlog</li>
<li>slave使用账号连接master</li>
</ol>
<h2 id="主从配置过程"><a href="#主从配置过程" class="headerlink" title="主从配置过程"></a>主从配置过程</h2><h3 id="编辑master的配置文件my-cnf"><a href="#编辑master的配置文件my-cnf" class="headerlink" title="编辑master的配置文件my.cnf"></a>编辑master的配置文件my.cnf</h3><pre><code>#1.配置server-id
server-id=101

#2.开启二进制日志
log-bin=mysql-bin

#3.指定二进制日志格式
binlog-format=mixed
</code></pre><blockquote>
<p>二进制日志格式binlog-format</p>
</blockquote>
<ol>
<li>statement：二进制记录的是执行语句，update insert…</li>
<li>row：二进制执行的是磁盘变化</li>
<li>mixed：混合的，由系统根据语句决定</li>
</ol>
<blockquote>
<p>到底使用哪种format，根据实际情况判断</p>
</blockquote>
<p><strong>比如：</strong><br>磁盘变化少，语句长，使用row<br><code>update user set age = age + 1 where id = 2;</code><br>磁盘变化多，语句短，使用statement<br><code>update slary = slary + 1000;</code> (10w条数据)</p>
<h3 id="编辑slave的配置文件my-cnf"><a href="#编辑slave的配置文件my-cnf" class="headerlink" title="编辑slave的配置文件my.cnf"></a>编辑slave的配置文件my.cnf</h3><pre><code>#1.配置server-id
server-id=102

#2.开启二进制日志(备份)
log-bin=mysql-bin

#3.创建relay日志
relay-log=mysql-relay
</code></pre><blockquote>
<p>配置完成后重启两个机器的mysql服务器<br><code>service mysqld restart</code></p>
</blockquote>
<h2 id="启动主从建立关系"><a href="#启动主从建立关系" class="headerlink" title="启动主从建立关系"></a>启动主从建立关系</h2><h3 id="master的操作"><a href="#master的操作" class="headerlink" title="master的操作"></a>master的操作</h3><p>查看主服务器的状态<br><code>show master status</code></p>
<pre><code>#授权账号密码
grant replication clinet,replication slave on *.* to test@&#39;192.168.1.%&#39; identified by &#39;123qwe&#39;;

flush privileges;
</code></pre><h3 id="slave的操作"><a href="#slave的操作" class="headerlink" title="slave的操作"></a>slave的操作</h3><p>建立连接</p>
<pre><code>change master to
master_host=&#39;192.168.1.101&#39;
master_user=&#39;test&#39;
master_password=&#39;123qwe&#39;
master_log_file=&#39;mysql-bin.000001&#39;
master_log_pass=348
</code></pre><p>查看连接状态<br><code>show slave status</code></p>
<p>启动从服务器同步进程并查看状态<br><code>start slave</code></p>
<blockquote>
<p>如果报错Last_IO_Error: error connect to master ‘test@192.168.1.101:3306’ ……<br>使用Telnet 192.168.1.101 3306排查连接master是否成功<br>有可能是防火墙开了，关闭防火墙：<code>services iptables stop</code></p>
</blockquote>
<pre><code>stop slave;
start slave;
show slave status;
</code></pre><h3 id="验证测试"><a href="#验证测试" class="headerlink" title="验证测试"></a>验证测试</h3><p>在mater中创建数据库和表，并做各种增删改操作，到从库观察验证</p>
<h2 id="补充说明"><a href="#补充说明" class="headerlink" title="补充说明"></a>补充说明</h2><p><strong>mysql经常报错如下：</strong><br><code>can&#39;t connect to local mysql server through socket &#39;/tmp/mysqld.sock&#39;</code><br>通过<code>/tmp/mysqld.sock</code>下的socket文件连接不成功，比如我们默认安装的mysql通过配置文件可以发现mysql.sock在<code>/var/lib/mysql/mysql.sock</code>下。也就是说/tmp/下没有mysqld.sock文件</p>
<p><strong>有两种解决方法：</strong></p>
<ol>
<li>指定sock文件位置<br><code>mysql -uroot -p -S /var/lib/mysql/mysql.sock</code></li>
<li>建立一个软连接<br><code>ln -s /var/lib/mysql/mysql.sock /tmp/mysqld.sock</code></li>
</ol>
<h2 id="后续学习计划"><a href="#后续学习计划" class="headerlink" title="后续学习计划"></a>后续学习计划</h2><p>主从复制可能会出现的问题级解决方案<br>mysql读写分离策略</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[vim学习笔记]]></title>
      <url>http://upeng.github.io/2016/09/11/vim/</url>
      <content type="html"><![CDATA[<blockquote>
<p>还是要经常折腾一下</p>
</blockquote>
<p>最近稍微折腾了一下vim，大家都知道linux环境下如果vim操作的足够熟练，完全可以替代各种文本编辑器，然而自己对vim的操作仍停留在最原始和基础的命令，只会简单的增删改查，比如删除d，进入编辑模式i，复制粘贴y&amp;p，代码上下翻阅左右箭头，看到这你估计都惊呆了，怎么这么low!也是由于近期经常需要到仿真机上修改代码，发现自己vim编辑效率超级低，这才想着好好过一遍vim指令，提高逼格！^_^</p>
<h3 id="vim配置文件-vimrc"><a href="#vim配置文件-vimrc" class="headerlink" title="vim配置文件.vimrc"></a>vim配置文件.vimrc</h3><p>这个文件是vim的配置文件，在这里配置一下全局生效，更加丰富的配置后续逐步学习</p>
<ol>
<li>比如之前每次查代码行数都要 <code>:set nu</code>，如果写在.vimrc就不用每次都<code>:set nu</code>了。</li>
<li>比如写完一行代码enter换行后，又从头再来，必须手动tab缩进。。。</li>
</ol>
<pre><code>set number # 显示行号
set autoindent # 自动缩进
</code></pre><a id="more"></a>
<h3 id="vim模式"><a href="#vim模式" class="headerlink" title="vim模式"></a>vim模式</h3><p>我们知道vim的模式【insert模式、普通模式、visual模式】中<strong>insert模式</strong>和<strong>visual模式</strong>是不能直接互跳的，必须先返回到<strong>普通模式</strong>！普通模式下<code>:</code>进入命令模式<br>快速进入insert模式：普通模式下输入 i/o/a，esc返回到普通模式<br>快速进入visual模式：普通模式下输入 v，再次v返回到普通模式</p>
<h3 id="普通模式下快速跳转"><a href="#普通模式下快速跳转" class="headerlink" title="普通模式下快速跳转"></a>普通模式下快速跳转</h3><p>不要只会箭头上下左右了！这个能提高效率</p>
<pre><code>gg(1G)  # 文件顶部 
G     # 文件尾部
control + [    # 向上片段移动
control + ] # 向下片段移动 
j # 下一行
k # 上一行
w # 下一个单词
b # 上一个单词
</code></pre><h3 id="复制粘贴一块代码"><a href="#复制粘贴一块代码" class="headerlink" title="复制粘贴一块代码"></a>复制粘贴一块代码</h3><ol>
<li>按Esc切换到正常模式；</li>
<li>把光标移到你需要复制的代码行首；</li>
<li>按V选择整行，可移动光标选择多行；</li>
<li>按d剪切或按y复制选择的代码；</li>
<li>移动光标到你需要粘贴的位置，按p粘贴代码到光标后的位置，或按P粘贴到光标前。</li>
</ol>
<h3 id="删除"><a href="#删除" class="headerlink" title="删除"></a>删除</h3><blockquote>
<p>下面命令必须先将鼠标移动到指定位置或指定行</p>
</blockquote>
<ul>
<li>字母<code>x</code> 删除当前定位的字母</li>
<li><code>dw</code> 删除当前鼠标所在的单词 <code>db</code>向前删除一个单词  </li>
<li><code>5dd</code>删除从当前行开始（包括当前行在内）的5行</li>
<li><code>dG</code> 输入<code>d</code>，然后<code>shift+g</code>(或大写<code>G</code>)</li>
<li>删除21-94行之间的内容，鼠标在第10行，如何快速删除！记住！总是忘记！<code>:21,94d</code></li>
</ul>
<h3 id="命令模式"><a href="#命令模式" class="headerlink" title="命令模式"></a>命令模式</h3><p>普通模式下<code>/</code>搜索，n下一个，N上一个<br>普通模式下<code>:e filepath/file</code>打开file<br>批量替换<code>:%s/word1/word2/g</code><br>复制一个文件全部内容到当前文件的指定位置<code>:read filepath/file</code></p>
<h3 id="撤销或重做"><a href="#撤销或重做" class="headerlink" title="撤销或重做"></a>撤销或重做</h3><p><code>u</code>撤销，<code>ctrl+r</code>重做（像sublime的cmd+y）</p>
<h3 id="快速进入insert模式"><a href="#快速进入insert模式" class="headerlink" title="快速进入insert模式"></a>快速进入insert模式</h3><p>普通模式下，随处都可以进入编辑模式，可能之前你只注意到<code>i</code>命令，其实还有两个<strong>非常实用</strong>的命令<code>a</code>和<code>o</code><br><code>a</code>–从当前鼠标定位开始,立刻可编辑<br><code>o</code>–立刻换行编辑</p>
<h3 id="代码注释"><a href="#代码注释" class="headerlink" title="代码注释"></a>代码注释</h3><blockquote>
<p>代码注释：</p>
</blockquote>
<ul>
<li>按v切换到可视化模式；</li>
<li>移动光标(j或k)选中需要注释的行的开头；</li>
<li>按大写I，然后输入注释符，如#；</li>
<li>最后按Esc。</li>
</ul>
<blockquote>
<p>取消注释：</p>
</blockquote>
<ul>
<li>按Ctrl+v切换到可视化模式；</li>
<li>按j或k选择要删除的注释符；</li>
<li>按d或x删除注释符</li>
</ul>
<p>通过学习，效率有所提高，但vim更加丰富的功能还需要进一步探索，计划上面的命令使用非常熟练自如后再进行第二阶段的学习和探索–命令模式和目录可视化的打造。</p>
<h3 id="vim键盘图"><a href="#vim键盘图" class="headerlink" title="vim键盘图"></a>vim键盘图</h3><p><img src="https://raw.githubusercontent.com/upeng/upeng.github.io/master/image/vim-keyboard.png" alt="vim-keyboard"></p>
<h3 id="参考文章"><a href="#参考文章" class="headerlink" title="参考文章"></a>参考文章</h3><p><a href="https://www.shiyanlou.com/questions/3176" target="_blank" rel="external">https://www.shiyanlou.com/questions/3176</a><br><a href="http://harttle.com/2015/11/07/vim-cursor.html" target="_blank" rel="external">http://harttle.com/2015/11/07/vim-cursor.html</a></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Mac下安装LNMP(Nginx+PHP5.6)环境]]></title>
      <url>http://upeng.github.io/2016/08/28/mac-lnmp/</url>
      <content type="html"><![CDATA[<h2 id="安装Homebrew"><a href="#安装Homebrew" class="headerlink" title="安装Homebrew"></a>安装Homebrew</h2><p>从零开始安装Mac下LNMP环境的过程</p>
<blockquote>
<p>确保系统已经安装xcode，然后使用一行命令安装依赖管理工具Homebrew</p>
</blockquote>
<p>安装完xcode之后可以使用</p>
<pre><code>brew install FORMULA
</code></pre><p>来安装所需要的依赖了.更详细的信息参考Homebrew的[官方Cookbook][1]</p>
<p>因此使用Homebrew常见的流程是：<br><a id="more"></a></p>
<ol>
<li>增加一个程序源（新增一个水龙头） brew tap homebrew/php</li>
<li>更新程序源 brew update</li>
<li>安装程序包（按照配方酿酒） brew install git</li>
<li>查看配置 brew config 可以看到程序包默认安装在/usr/local/Cellar下 （酒桶放在地窖内）</li>
</ol>
<h2 id="安装PHP5-6（FPM方式）"><a href="#安装PHP5-6（FPM方式）" class="headerlink" title="安装PHP5.6（FPM方式）"></a>安装PHP5.6（FPM方式）</h2><p>首先加入Homebrew官方的几个软件源</p>
<pre><code>brew tap homebrew/dupes
brew tap homebrew/versions
brew tap homebrew/php
</code></pre><p>PHP如果采用默认配置安装，会编译mod_php模块并只运行在Apache环境下，为了使用Nginx，这里需要编译php-fpm并且禁用apache，主要通过参数<code>--without-fpm --without-apache</code>来实现。完整的安装指令为</p>
<pre><code>brew install php56 \
--without-snmp \
--without-apache \
--with-debug \
--with-fpm \
--with-intl \
--with-homebrew-curl \
--with-homebrew-libxslt \
--with-homebrew-openssl \
--with-imap \
--with-mysql \
--with-tidy
</code></pre><p>由于OSX已经自带了PHP5.5环境，因此需要修改系统路径，优先运行brew安装的版本，在<code>~/.bashrc</code>里加入：</p>
<pre><code>export PATH=&quot;/usr/local/bin:/usr/local/sbin:$PATH&quot;
</code></pre><blockquote>
<p>这时运行 php -v查看版本是否是5.6<br>运行php -m 查看PHP安装都扩展列表</p>
</blockquote>
<p>如果要安装新的php扩展，可以直接安装而不用每次重新编译php，所有的扩展可以通过</p>
<pre><code>brew search php56
</code></pre><p>看到，下面是我自己所需要的扩展，可以支持Phalcon框架：</p>
<pre><code>brew install php56-gearman php56-msgpack php56-memcache php56-memcached php56-mongo  php56-phalcon php56-redis php56-xdebug
</code></pre><h3 id="PHP-FPM的加载与启动"><a href="#PHP-FPM的加载与启动" class="headerlink" title="PHP-FPM的加载与启动"></a>PHP-FPM的加载与启动</h3><p>php-fpm.conf  php.ini</p>
<pre><code>/usr/local/etc/php/5.6
</code></pre><p>php-fpm进程</p>
<pre><code>/usr/local/sbin
</code></pre><p><strong>安装完毕后可以通过以下指令启动和停止php-fpm</strong></p>
<pre><code>./php-fpm -D    //进入sbin目录

killall php-fpm
</code></pre><p>同时可以将php-fpm加入开机启动</p>
<pre><code>ln -sfv /usr/local/opt/php56/*.plist ~/Library/LaunchAgents

launchctl load ~/Library/LaunchAgents/homebrew.mxcl.php56.plist
</code></pre><h3 id="查看php-fpm进程"><a href="#查看php-fpm进程" class="headerlink" title="查看php-fpm进程"></a>查看php-fpm进程</h3><p>查看9000端口<br><code>lsof -i:9000</code><br>或者<br><code>ps -ef | grep php-fpm</code></p>
<h2 id="安装Nginx"><a href="#安装Nginx" class="headerlink" title="安装Nginx"></a>安装Nginx</h2><pre><code>brew install nginx
</code></pre><p>安装完毕后可以通过</p>
<pre><code>nginx
nginx -s quit
</code></pre><p>启动和关闭，同时也支持重载配置文件等操作</p>
<pre><code>nginx -s reload|reopen|stop|quit
</code></pre><p>nginx安装后默认监听8080端口，可以访问<a href="http://localhost:8080" target="_blank" rel="external">http://localhost:8080</a> 查看状态。如果要想监听80端口需要root权限，运行</p>
<pre><code>sudo chown root:wheel /usr/local/Cellar/nginx/1.6.2/bin/nginx
sudo chmod u+s /usr/local/Cellar/nginx/1.6.2/bin/nginx
</code></pre><blockquote>
<p>我是直接在nginx配置中改成80端口，然后重启生效<br>并使用root权限启动</p>
</blockquote>
<pre><code>sudo nginx
</code></pre><p>开机启动</p>
<pre><code>ln -sfv /usr/local/opt/nginx/*.plist ~/Library/LaunchAgents

launchctl load ~/Library/LaunchAgents/homebrew.mxcl.nginx.plist
</code></pre><h2 id="Nginx-PHP-FPM配置"><a href="#Nginx-PHP-FPM配置" class="headerlink" title="Nginx + PHP-FPM配置"></a>Nginx + PHP-FPM配置</h2><p>好文：<a href="http://www.nginx.cn/nginx-how-to" target="_blank" rel="external">http://www.nginx.cn/nginx-how-to</a></p>
<p>Nginx一般都会运行多个域名（虚拟主机）</p>
<blockquote>
<p>按Ubuntu的文件夹结构来存放Nginx的配置文件</p>
</blockquote>
<pre><code>mkdir -p /usr/local/var/logs/nginx
mkdir -p /usr/local/etc/nginx/sites-available
mkdir -p /usr/local/etc/nginx/sites-enabled
mkdir -p /usr/local/etc/nginx/conf.d
mkdir -p /usr/local/etc/nginx/ssl
</code></pre><blockquote>
<p>联想到work中，vhost和fpm.d目录，新增域名与目录时，只需要增加两个配置文件；前提时php-fpm.conf和nginx.conf主配置文件中都有</p>
</blockquote>
<pre><code>include vhost/*.conf
include fpm.d/*.conf
</code></pre><p><strong>上述都conf.d等同于fpm.d，sites-enabled等同于vhost</strong></p>
<h3 id="编辑Nginx全局配置"><a href="#编辑Nginx全局配置" class="headerlink" title="编辑Nginx全局配置"></a>编辑Nginx全局配置</h3><pre><code>vim /usr/local/etc/nginx/nginx.conf
</code></pre><pre><code>worker_processes  1;
error_log   /usr/local/var/logs/nginx/error.log debug;
pid        /usr/local/var/run/nginx.pid;

events {
    worker_connections  256;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    log_format main &#39;$remote_addr - $remote_user [$time_local] &#39;
        &#39;&quot;$request&quot; $status $body_bytes_sent &#39;
        &#39;&quot;$http_referer&quot; &quot;$http_user_agent&quot; &#39;
        &#39;&quot;$http_x_forwarded_for&quot; $host $request_time $upstream_response_time $scheme &#39;
        &#39;$cookie_evalogin&#39;;

    access_log  /usr/local/var/logs/access.log  main;

    sendfile        on;
    keepalive_timeout  65;
    port_in_redirect off;

    include /usr/local/etc/nginx/sites-enabled/*;
}
</code></pre><p>这样一来首先可以把一些可复用配置独立出来放在<code>/usr/local/etc/nginx/conf.d</code>下，比如fastcgi的设置就可以独立出来</p>
<pre><code>vim /usr/local/etc/nginx/conf.d/php-fpm
</code></pre><p>内容为</p>
<pre><code>location ~ \.php$ {
    try_files                   $uri = 404;
    fastcgi_pass                127.0.0.1:9000;
    fastcgi_index               index.php;
    fastcgi_intercept_errors    on;
    include /usr/local/etc/nginx/fastcgi.conf;
}
</code></pre><p>然后<code>/usr/local/etc/nginx/sites-enabled</code>目录下可以一个文件对应一个域名的配置，比如web服务器目录是<code>/opt/htdocs</code></p>
<pre><code>vim /usr/local/etc/nginx/sites-enabled/default
</code></pre><pre><code>server {
    listen       80;
    server_name  localhost;
    root         /opt/htdocs/;

    location / {
        index  index.html index.htm index.php;
        include     /usr/local/etc/nginx/conf.d/php-fpm;
    }
}
</code></pre><p>此时启动了php-fpm并且启动了Nginx后，就可以通过<a href="http://localhost来运行php程序了" target="_blank" rel="external">http://localhost来运行php程序了</a></p>
<h2 id="安装MySQL"><a href="#安装MySQL" class="headerlink" title="安装MySQL"></a>安装MySQL</h2><pre><code>brew install mysql
</code></pre><p>可以通过</p>
<pre><code>mysql.server start
mysql.server stop
</code></pre><p>来启动／停止，启动后默认应为空密码，可以通过mysqladmin设置一个密码</p>
<pre><code>mysqladmin -uroot password &quot;mypassword&quot;
</code></pre><p>但是在操作的时候出现了空密码无法登入的情况，最终只能通过mysqld_safe来设置</p>
<pre><code>sudo mysqld_safe --skip-grant-tables
mysql -u root
mysql&gt; UPDATE mysql.user SET Password=PASSWORD(&#39;mypassword&#39;) WHERE User=&#39;root&#39;;
mysql&gt; FLUSH PRIVILEGES;
</code></pre><p><strong>依旧报错</strong></p>
<blockquote>
<p><strong>坑1</strong>：一直出现mysql.sock错误，<code>mysql -uroot -p</code>;<br><strong>填坑1</strong>：<code>mysql -h 127.0.0.1 -u root -p</code>  why?</p>
<p><strong>坑2</strong>：设置密码报错：<code>Password</code>字段不存在<br><strong>填坑2</strong>：mysql5.7已将<code>Password</code>字段去掉，改成<code>authentication_string</code>；所以正确都姿势如下：</p>
<pre><code>update mysql.user set authentication_string=password(&#39;123qwe&#39;) where user=&#39;root
</code></pre><p>老版本mysql没变</p>
</blockquote>
<p>最后将MySQL加入开机启动</p>
<pre><code>cp /usr/local/Cellar/mysql/5.6.22/homebrew.mxcl.mysql.plist ~/Library/LaunchAgents/
</code></pre><h2 id="查看服务列表"><a href="#查看服务列表" class="headerlink" title="查看服务列表"></a>查看服务列表</h2><pre><code>brew services list
</code></pre><pre><code>Name  Status  User    Plist
mysql started root    /Library/LaunchDaemons/homebrew.mxcl.mysql.plist
nginx started yupeng6 /Users/yupeng6/Library/LaunchAgents/homebrew.mxcl.nginx.plist
php56 started yupeng6 /Users/yupeng6/Library/LaunchAgents/homebrew.mxcl.php56.plist
</code></pre><p>安装redis和memcache过程省略，详见下面博文，本文也是参考此<br><a href="http://avnpc.com/pages/install-lnmp-on-osx" target="_blank" rel="external">http://avnpc.com/pages/install-lnmp-on-osx</a></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[首次折腾Mac]]></title>
      <url>http://upeng.github.io/2016/08/28/mac-primary/</url>
      <content type="html"><![CDATA[<h3 id="常用快捷键"><a href="#常用快捷键" class="headerlink" title="常用快捷键"></a>常用快捷键</h3><ul>
<li>delete（backspac）</li>
<li>fn＋delete（后删）</li>
<li>option＋v ＝ √</li>
<li>command ＋ space ＝ spotlight</li>
<li>option ＋ space ＝ hotkey window 呼起item2</li>
<li>control ＋ space ＝ 输入法切换</li>
</ul>
<h3 id="开发配置"><a href="#开发配置" class="headerlink" title="开发配置"></a>开发配置</h3><p>查看<strong>Mac开发配置手册</strong>省了很多时间<br><a href="https://aaaaaashu.gitbooks.io/mac-dev-setup/content/" target="_blank" rel="external">https://aaaaaashu.gitbooks.io/mac-dev-setup/content/</a><br>iTem2文档也是相当之多<br><a href="https://leohxj.gitbooks.io/a-programmer-prepares/content/software/mac/softwares/iTerm2.html" target="_blank" rel="external">https://leohxj.gitbooks.io/a-programmer-prepares/content/software/mac/softwares/iTerm2.html</a><br><a id="more"></a></p>
<blockquote>
<p><strong>坑</strong>：在App Store中安装xcode，总是提示第一次使用，需要输入一堆东西，然而没有什么卵用，浪费了我好多时间。果断去<code>https://developer.apple.com/xcode/</code>下载,下载Xcode command line tools即可，只有几百M.</p>
</blockquote>
<h4 id="安装homebrew"><a href="#安装homebrew" class="headerlink" title="安装homebrew"></a>安装homebrew</h4><p>mac下的包管理工具，类似于ubuntu的apt-get<br>brew install nginx ＝ apt-get install nginx</p>
<h4 id="安装iTem2"><a href="#安装iTem2" class="headerlink" title="安装iTem2"></a>安装iTem2</h4><blockquote>
<p><strong>坑</strong>：字体乱码<br>安装字体即可：monaco for powerline下载安装完成后在设置中设为该字体即可</p>
</blockquote>
<ul>
<li>hotkey window      //option ＋ space启动</li>
<li>default           //dock中的快速启动<br>二者均可在<code>profiles/open profies/edit profiles</code>; 设置<code>color，text</code>等等</li>
</ul>
<h3 id="hostname"><a href="#hostname" class="headerlink" title="hostname"></a>hostname</h3><p><code>hostname</code>  //查看hostname<br><code>sudo scutil --set HostName MacBook-pro</code>   //修改hostname</p>
<h4 id="安装开发环境LNMP"><a href="#安装开发环境LNMP" class="headerlink" title="安装开发环境LNMP"></a>安装开发环境LNMP</h4><p>鼓捣了好久，最后还是按照下面这个教程来操作了，从零开始安装Mac下的LNMP环境的过程</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[laravel基础任务学习清单]]></title>
      <url>http://upeng.github.io/2016/08/15/laravel-basic/</url>
      <content type="html"><![CDATA[<h2 id="关于Restful路由"><a href="#关于Restful路由" class="headerlink" title="关于Restful路由"></a>关于Restful路由</h2><p><code>Route::resource(&#39;task&#39;, &#39;TaskController&#39;);</code><br>产生多条路由，<code>POST/DELTE/PUT/GET</code>，对应着<code>TaskController</code>的各个方法；<br>对于laravel5.2，使用 <code>php artisan make:controller TaskController --resource</code>可产生默认的多种方法<br>通过<code>php artisan route:list</code>可以显示已经注册的路由列表</p>
<p><img src="https://raw.githubusercontent.com/upeng/upeng.github.io/master/image/laravel-route-list.png" alt="laravel-route-list"></p>
<a id="more"></a>
<h2 id="表单验证"><a href="#表单验证" class="headerlink" title="表单验证"></a>表单验证</h2><blockquote>
<p>store、update方法常使用</p>
</blockquote>
<p>基地控制器<code>Controller</code>内使用了<code>ValidatesRequests</code>这个<code>trait</code>，因此他为所有的控制器都提供了方便的<code>validate</code>验证方法。</p>
<blockquote>
<p>调研了解trait机制，为什么TestController use了这个trait，TestController 就可以使用ValidatesRequests的validate方法</p>
</blockquote>
<h3 id="最原始的表单验证（Controller中）"><a href="#最原始的表单验证（Controller中）" class="headerlink" title="最原始的表单验证（Controller中）"></a>最原始的表单验证（Controller中）</h3><p>validate方法会验证接受HTTP传入的请求与验证规则，如果验证通过，我们的代码才会继续向下运行。如果验证失败会抛出异常错误返回给用户。</p>
<pre><code>public function store(Request $request)
{
    //验证用户请求
    $this-&gt;validate($request,[
        &#39;name&#39; =&gt; &#39;required&#39;,
        &#39;content&#39; =&gt; &#39;required|min:5&#39;,
    ]);

    //通过验证，保存至数据库
    Task::create($request);
}
</code></pre><p><strong>说明</strong>：我们将验证规则传递到<code>validate</code>方法中，如果验证失败，则自动生成一个对应的响应。并将验证的错误提醒闪存至<code>session</code>，也就是<code>$errors</code>。当验证失败时，用户将被重定向到我们的控制器 <code>create</code>方法，让我们在视图中显示错误的消息：</p>
<pre><code>@if (count($errors) &gt; 0)
    &lt;div class=&quot;alert alert-danger&quot;&gt;
        &lt;ul&gt;
            @foreach ($errors-&gt;all() as $error)
            &lt;li&gt;{{$error}}&lt;/li&gt;
        &lt;/ul&gt;
    &lt;/div&gt;
@endif
</code></pre><p><strong>补充</strong>：正常情况下我们想要把错误显示在对应的字段旁边，而不是一起显示在一个列表中；我们打印<code>session</code>中的<code>$errors-&gt;all()</code>是数字索引数组;</p>
<blockquote>
<p>要查看特定字段的第一个错误消息【比较常见的场景】</p>
</blockquote>
<p><strong>方法如下：</strong></p>
<pre><code>@if ($errors-&gt;has(&#39;name&#39;))
$errors-&gt;first(&#39;name&#39;);
@endif

@if ($error-&gt;has(&#39;content&#39;))
$error-&gt;first(&#39;content&#39;);
@endif
</code></pre><blockquote>
<p>我们想要得到一个字段对应的所有error</p>
</blockquote>
<p><strong>方法如下</strong>：</p>
<pre><code>foreach ($error-&gt;get(&#39;content&#39;) as $message)
{
    //
}
</code></pre><h3 id="表单请求验证，自定义请求类"><a href="#表单请求验证，自定义请求类" class="headerlink" title="表单请求验证，自定义请求类"></a>表单请求验证，自定义请求类</h3><blockquote>
<p>php artisan make request TaskFormReuest;</p>
</blockquote>
<p>这时会在<code>Http/Requests</code>目录生成<code>TaskFormReuest.php</code>,修改<code>rules</code>方法，</p>
<pre><code>//权限判断
public function authorize()
{
    return true;
}

//验证规则
public function rules()
{
    return [
        &#39;name&#39; =&gt; &#39;required|min:5&#39;,
    ];
}

//也可以自定义message方法
public function messages()
{
    return [
        &#39;title.required&#39; =&gt; &#39;标题是必填的&#39;,
        &#39;body.required&#39;  =&gt; &#39;消息是必填的&#39;,
    ];
}

//messages还可以这样动态设置
public function messages()
{
    return [
        &#39;required&#39; =&gt; &#39;:attribute 是必填字段&#39;,
        &#39;min&#39;    =&gt; &#39;:attribute 长度不小于 :min&#39;,
    ];
}
</code></pre><h3 id="手动创建验证程序"><a href="#手动创建验证程序" class="headerlink" title="手动创建验证程序"></a>手动创建验证程序</h3><p>不使用validatesRequests的validate方法<br>我们可以use Validator类，手动创建一个validator实例，并通过Validator::make()生成一个新的validator</p>
<pre><code>use Validator;
...
public function store(Request $request)
{
    $validator = Validtor::make($request-&gt;all(),[
        &#39;name&#39; =&gt; &#39;requeire&#39;,
        &#39;content&#39; =&gt; &#39;require|min:6&#39;,
    ]);

    if ($validator-&gt;fails())
    {
        return redirect()-&gt;action(&#39;TaskController@index&#39;)
        -&gt;withErrors($validator)
                -&gt;withInput();
    }
}
</code></pre><p><strong>注</strong>：<code>Validator::make($param1, $param2)</code>，其中<code>$param1</code>是验证的数据，<code>$params</code>是规则</p>
<h2 id="model操作"><a href="#model操作" class="headerlink" title="model操作"></a>model操作</h2><p>增：</p>
<pre><code>1. Task::create($request);
2. 
$task = new Task();
$task-&gt;name = $request-&gt;name;
$task-&gt;created_at = Carbon::now();
$task-&gt;save();
</code></pre><p>删：</p>
<pre><code>1.Task::destroy($id);
2.Task::findOrFail($id)-&gt;delete();
</code></pre><p>改:</p>
<pre><code>Task::update($request, $id);
</code></pre><p>查：</p>
<pre><code>1.Task::all();
2.Task::where()-&gt;get();
3.Task::orderBy(&#39;created_at&#39;, &#39;desc&#39;)-&gt;get();
</code></pre><h2 id="factory工厂模型使用"><a href="#factory工厂模型使用" class="headerlink" title="factory工厂模型使用"></a>factory工厂模型使用</h2><ul>
<li><code>Task Model</code>中设置  <code>protected $fillable = [&#39;name&#39;, &#39;created_at&#39;]</code>;</li>
<li><p><code>datebase/factories/ModelFactory.php</code>新增</p>
<pre><code>$factory-&gt;define(App\Task::class, function (Faker\Generator $faker) {
  return [
      &#39;name&#39; =&gt; $faker-&gt;sentence,
      &#39;created_at&#39; =&gt; Carbon\Carbon::now(),
  ];
});
</code></pre></li>
<li><p><code>php artisan tinker</code></p>
<pre><code>namespace App;
factory(App\Task::class,5)-&gt;create();
</code></pre></li>
</ul>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[vuejs与后台数据的交互实践]]></title>
      <url>http://upeng.github.io/2016/07/17/vuejs/</url>
      <content type="html"><![CDATA[<p>Vuejs是MVVM框架，数据和DOM视图双向绑定，数据驱动的 web 界面的库。</p>
<p><strong>响应的数据绑定</strong>：数据与DOM保持同步非常简单，数据发生变化的时候，如果我们使用JQuery操作DOM的话自然是可行的，但是jQuery操作DOM代码复杂容易出错，就拿一个简单的例子，在列表中新增一行数据，使用JQuery可能会刷新整个DOM,并对页面refresh。但是vue实现数据与DOM的绑定后，只要数据发生变化，就自动改动DOM，所以我们的工作重点就是修改数据，不必与DOM更新搅在一起。</p>
<p><strong>我的理解</strong>：Vuejs简化了操作DOM或者不再去手动操作DOM，一个简单的API就搞定了DOM的自动更新，让我们脱离了使用Js操作DOM的复杂流程。这也是Vue的迷人之处吧，其他的优点诸如：组件化、模块化、体积小等优点我就不再罗列了，本文重点也不在此。<br><a id="more"></a><br>那么问题来了</p>
<h2 id="vuejs与后端（比如PHP）是如何交互的？"><a href="#vuejs与后端（比如PHP）是如何交互的？" class="headerlink" title="vuejs与后端（比如PHP）是如何交互的？"></a>vuejs与后端（比如PHP）是如何交互的？</h2><p>怎么使用Vuejs与后台数据（这里是指后台的数据库，不是vuejs的model，因为我理解的vuejs的model仍是前端数据）交互？其实这个问题本身就存在问题，（这是最初是对vuejs不怎么了解的情况下做出的提问），事实上vuejs <strong>关注点</strong>是如何优雅的操作DOM或者减少手动操作DOM，至于与后台的交互，这是js（当然vuejs也是基于js啊）要做的事情，所以<strong>正确的提问姿势</strong>应该是：<em>js怎样和后端交互？</em> 那么现在这个问题就变得简单了。我们知道js是不能直接操作数据库的，但是ajax却架起前后端的桥梁，能让前后端数据进行交互。</p>
<blockquote>
<p>当然vuejs作为js衍生出的框架，它除了具有js的特性外，还有自己的特色呢，所以vuejs可以使用vue-resource来与后端交互。</p>
</blockquote>
<p>下面使用ajax(基于jquery)、vuejs、php实现简单的数据的增删改查。</p>
<pre><code>var url = &#39;http://localhost:8080/vuejs/api/book.php&#39;;
getJson(url, pushDom);
</code></pre><p>分析getJson函数【已经封装】</p>
<ol>
<li>初始化信息列表</li>
<li>两个参数：<br>（1）第一个是获取列表的接口地址url；<br>（2）第二个是回调函数名</li>
<li>流程分析：<br>在getJson中通过ajax调取接口url获取数据data，然后将data作为参数调用pushDom函数，pushDom是展示、或者使用vue操作DOM的功能函数。那么怎么使用jquey、ajax配合vue进行后端数据库增删改查。<br>首先需要对vue的增删改查机制了如指掌才行；methods事件触发，我们可以在methods中定义add、delete、modify完成数据的绑定，异步刷新显示；同时将获取的数据传递给封装好的ajax函数中，与后端交互【正确的流程应该与后台交互成功才会自动更新DOM，这里不细论】；例如<br><strong>添加数据</strong>：<pre><code>methods:{
 addBook:function(){
     this.book.id=parseInt(postData(this.book));
     this.books.push(this.book);
     this.book=&#39;&#39;;
 }
}
</code></pre>我们从前端拿到的数据<code>this.book</code>，然后调用<code>postData(this.book)</code>;<code>postData</code>就是一个与后端交互的封装了ajax的函数<br>期间遇到了一点小意外，总是获取不到ajax的返回值，关于如何获取ajax返回值和为何获取不到ajax中的值问题，<code>postData</code>函数中写的很清楚<br><code>async: false</code>, 默认是<code>true</code></li>
</ol>
<p>代码见：<a href="https://github.com/upeng/learn-vuejs" target="_blank" rel="external">github</a><br>demo页：<img src="https://raw.githubusercontent.com/upeng/upeng.github.io/master/image/vuedemo.png" alt="demo"></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[vagrant-快速搭建linux开发环境]]></title>
      <url>http://upeng.github.io/2016/07/13/vagrant/</url>
      <content type="html"><![CDATA[<p>平时开发测试使用公司提供的弹性开发机（Centos），自己想折腾一些东西又不方面在上面部署,鉴于</p>
<ul>
<li>学习linux考虑（比如经常装扩展插件啥的），装WAMP有些low而且不适合扩展。</li>
<li>在windows上装linux虚拟机（VMare等）又太占内存（o(╯□╰)o我的笔记本只有4G内存。。。）</li>
</ul>
<p>有没有那种快捷的搭建linux开发环境的东东呢？答案是肯定的。偶然间发现了vagrant，它就像一个容器一样（Docker？），部署LAMP十分快捷；<br><a id="more"></a></p>
<h2 id="我所理解的vagrant-box"><a href="#我所理解的vagrant-box" class="headerlink" title="我所理解的vagrant box"></a>我所理解的vagrant box</h2><p>我理解的，vagrant box就是一个简易linux操作系统内核，到vagrant官网可以看到很多【<a href="http://www.vagrantbox.es/】，" target="_blank" rel="external">http://www.vagrantbox.es/】，</a> 选择一个box下载下来，然后通过vagrant add box就可以直接初始化一个linux系统了；如果你想创建多个linux系统，那就多add 几个，所以vagrant也可以练习分布式！但是，这个网站的速度也慢的可以，推荐一个国内镜像，还是挺快的，每秒20M左右 【<a href="http://mirrors.hypo.cn/ubuntu-vagrant/】" target="_blank" rel="external">http://mirrors.hypo.cn/ubuntu-vagrant/】</a></p>
<h2 id="预备软件"><a href="#预备软件" class="headerlink" title="预备软件"></a>预备软件</h2><p>virtualBox : 运行linux的环境 【<a href="https://www.vagrantup.com/】" target="_blank" rel="external">https://www.vagrantup.com/】</a><br>vagrant：提供一些vagrant命令 【<a href="https://www.virtualbox.org/】" target="_blank" rel="external">https://www.virtualbox.org/】</a><br>百度云盘备份：【<a href="http://pan.baidu.com/s/1o8aoofo】" target="_blank" rel="external">http://pan.baidu.com/s/1o8aoofo】</a></p>
<h2 id="vagrant-box下载"><a href="#vagrant-box下载" class="headerlink" title="vagrant box下载"></a>vagrant box下载</h2><p>记得第一次是“在线”下载vagrant box，执行vagrant init；vagrant up之后，当检测到无box时会自动下载；速度慢的惊人（虽然我开了vpn），而且在线安装的box直接就在我们vagrant up命令中执行了，没有见到box后缀的文件，当你重装系统后或者你安装的vagrant损坏后，难道又要重新在线安装box？ +_+ 所以建议下载box文件，然后使用vagrant add box命令</p>
<h2 id="vagrant-常用命令-快速搭建LAMP流程"><a href="#vagrant-常用命令-快速搭建LAMP流程" class="headerlink" title="vagrant 常用命令(快速搭建LAMP流程)"></a>vagrant 常用命令(快速搭建LAMP流程)</h2><h3 id="添加box"><a href="#添加box" class="headerlink" title="添加box"></a>添加box</h3><p>vargant box add  trusty  trusty-server-cloudimg-amd64-vagrant-disk1.box<br>// vagrant box add  box默认名称是base,此处我们定义为trusty  .box文件就是我们下载的box</p>
<h3 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h3><p>vagrant init<br>因为我们重新命名了我们的box为trusty，所以你再任何地方只要执行vagrant init，生成的Vagrant文件中config.vm.box=’trusty’；<br>如果执行vagrant up会失败，因为vagrant init默认以base为名初始化；正确的初始化姿势是<br>vagrant init trusty</p>
<h3 id="启动虚拟机"><a href="#启动虚拟机" class="headerlink" title="启动虚拟机"></a>启动虚拟机</h3><p>vargant up<br>vagrant up之前需要将Vagrant文件的第25行注释去掉，这样才能在windows中访问我们在linux中创建的程序；在trusty版本的Ubuntu中，有个目录 <code>/vagrant</code>与本地windows中Vagrant同级，因此可以本地编辑，linux中运行，就像磁盘映射。</p>
<h3 id="登录虚拟机"><a href="#登录虚拟机" class="headerlink" title="登录虚拟机"></a>登录虚拟机</h3><p>vagrant ssh<br>使用vagrant ssh进入Ubuntu系统；进入后需要更新升级Ubuntu的一些包和工具，同事需要安装我们的LAMP。<br>升级包命令： <code>sudo apt-get update</code><br>安装php apache: <code>sudo apt-get install apache2 php5 libapache2-mod-php5</code><br>安装mysql: <code>sudo apt-get install mysql-server mysql-client php5-mysql</code></p>
<h3 id="配置apache2"><a href="#配置apache2" class="headerlink" title="配置apache2"></a>配置apache2</h3><p>安装完成后访问：<code>http://localhost:8080</code> , 默认指向 <code>/var/www/html</code> ,修改配置文件如下：<br>配置文件路径  <code>/etc/apache2/sites-available/000-default.conf</code></p>
<pre><code>DocumentRoot /vagrant
&lt;Directory /vagrant&gt;
                Options -Indexes +followSymLinks +MultiViews
                AllowOverride all
                Require all granted
&lt;/Directory&gt;
</code></pre><p>修改完成后重启apache <code>service apache2 restart</code></p>
<p>参考：<br><a href="http://rrylee.github.io/2015/11/26/quick-start-linux-php/" target="_blank" rel="external">http://rrylee.github.io/2015/11/26/quick-start-linux-php/</a><br><a href="http://rmingwang.com/vagrant-commands-and-config.html" target="_blank" rel="external">http://rmingwang.com/vagrant-commands-and-config.html</a></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[MCQ学习笔记]]></title>
      <url>http://upeng.github.io/2016/07/11/memcacheq/</url>
      <content type="html"><![CDATA[<p>初次接触MCQ源于蓝v，之前也听同事说微博的消息推送也是使用MCQ。认识MCQ之前感觉它很神秘，用之后才发现并也不过如此，所以很多知识不能只凭想象或者言传，需要自身实践才可知晓。这里想记录一下自己使用MCQ的过程，以及MCQ的使用场景、优缺点等等。</p>
<blockquote>
<p>MemcacheQ - Simple Queue Service over Memcache<br>在memcache基础上的简单队列服务，所以它兼具memcache和队列的特点。</p>
</blockquote>
<a id="more"></a>
<h2 id="Features-特性"><a href="#Features-特性" class="headerlink" title="Features[特性]"></a>Features[特性]</h2><ul>
<li>damn simple（炒鸡简单)</li>
<li>very fast（非常快）-&gt; 给予memcache,在内存中操作</li>
<li>multiple queue（多队列)</li>
<li>concurrent well（并发good)</li>
<li>memcache protocol compatible（兼容memcache协议)</li>
</ul>
<p>memcacheq(MCQ)是轻量级的消息队列，依赖于Berkeley DB和libevent。Berkeley DB用于持久化存储队列数据，避免MCQ宕机数据丢失。</p>
<h2 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h2><blockquote>
<p>高并发，数据可以弱一致性（弱一致性？）。</p>
</blockquote>
<p>蓝v后台沿用之前橙v逻辑（数据量大，并发高），也使用了MCQ。在高并发，大数据量的情况下，请求过多会发生阻塞，试想，在这种情况下，如果大量请求同时对mysql进行增删改，则会产生很大行锁（InnoDB）或表锁（Myisam）;请求堆积过多还可能会出现too many connections的错误。使用了MCQ，可以异步处理，怎么异步法？请求在不断的往队列塞（入队列），队列也不断地去消费这些请求（出队列），当然消费能力也是在mysql能力范围之内的，为啥？【思考缓冲作用】二者就处于异步状态。</p>
<p>支持多队列，比如我们创建了队列A，B，C，他们可以同时工作，其实这也体现了并发异步的特性。</p>
<h2 id="使用方法"><a href="#使用方法" class="headerlink" title="使用方法"></a>使用方法</h2><p>因为MCQ是基于memcached开发，所以可以使用memcached的命令对它进行各种操作；客户端比如php可以像操作memcacheq那样操作MCQ</p>
<h3 id="启动MCQ"><a href="#启动MCQ" class="headerlink" title="启动MCQ"></a>启动MCQ</h3><p><code>/usr/local/memcacheq/bin/memcacheq -d -r 1024 -l 127.0.0.1 -p 22201 -u root</code></p>
<p><strong>参数说明</strong><br><code>-d</code> 以守护进程方式运行<br><code>-r</code> 最大化核心文件限制<br><code>-l</code> 监听网卡<br><code>-p</code> TCP监听端口<br><code>-u</code> 以（root）身份运行</p>
<h3 id="最常用的mcq命令：set和get"><a href="#最常用的mcq命令：set和get" class="headerlink" title="最常用的mcq命令：set和get"></a>最常用的mcq命令：set和get</h3><pre><code>telnet 127.0.0.1 22201 ....

set testqueue 0 0 3    //队列添加一条消息
xxx
STORED

set testqueue 0 0 5
xxxxx
STORED

stats queue
STAT testqueue 2/0

get testqueue
VALUE testqueue 0 3
xxx
END

stats queue
STAT testqueue 2/1
</code></pre><p><strong>注意</strong>：mcq没有expire time这个标记，是因为mcq支持数据持久化（Berkeley DB）。另外mcq存储长度length有限制（蓝v踩到的坑），之前同步前台用户认证申请材料至后台，因为材料增多，导致参数长度增加，超过了mcq的消息长度的限制：limit ? 后面分析查明</p>
<blockquote>
<p>MCQ和Redis的比较</p>
</blockquote>
<p><strong>memcacheq</strong></p>
<ul>
<li>数据保存在内存中，速度快</li>
<li>支持数据持久化</li>
<li>队列长度限制小</li>
<li>与memcache兼容</li>
<li>与memcached是两个独立服务，如同时使用需要分别安装</li>
</ul>
<p><strong>Redis</strong></p>
<ul>
<li>数据保存在内存中，速度快</li>
<li>支持数据持久化</li>
<li>队列长度限制小</li>
<li>支持多种数据类型</li>
<li>同时可以做为缓存来使用</li>
</ul>
<p>自己思考一下，在项目中MCQ和Redis各自的使用场景</p>
<p>后续阅读文章<br><a href="https://www.ibm.com/developerworks/cn/linux/l-ipc/" target="_blank" rel="external">深刻理解Linux进程间通信（IPC）</a><br><a href="http://blog.buaa.us/talk-about-mq/" target="_blank" rel="external">可靠消息队列浅谈</a><br><a href="http://www.fireflies.me/tag/%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97/" target="_blank" rel="external">MemcacheQ分析</a></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[使用zephir快速编写php扩展]]></title>
      <url>http://upeng.github.io/2016/06/30/zephir/</url>
      <content type="html"><![CDATA[<h2 id="zephir简介"><a href="#zephir简介" class="headerlink" title="zephir简介"></a>zephir简介</h2><p><a href="http://zephir-lang.com/" target="_blank" rel="external">zephir</a>开源，是<code>zend engine / php / intermediate</code>的缩写，其语法和php很相似，所以对于phper而言创建php扩展简单快捷。如果你不想使用C编写php扩展，可以考虑zephir，因为它很简单，学习成本低。<br><a id="more"></a></p>
<h2 id="zephir特点"><a href="#zephir特点" class="headerlink" title="zephir特点"></a>zephir特点</h2><p><code>dynamic/static</code> 变量类型可以是动态/静态变量<br><code>pointers or direct memory management aren’t allowed</code>不允许指针和直接的内存管理<br><code>ahead of time</code> 编译模型AOT<br><code>task-local garbage collection</code> 内存模型 Task-Local垃圾回收</p>
<blockquote>
<p>下面就以一个简单扩展开发为例，总结使用zephir开发php来扩展的流程！</p>
</blockquote>
<h2 id="安装zephir"><a href="#安装zephir" class="headerlink" title="安装zephir"></a>安装zephir</h2><ol>
<li>需要先安装相关的依赖</li>
<li>使用git的方式获取源代码进行安装</li>
<li>通过运行zephir命令验证安装是否成功<pre><code>$ git clone https://github.com/phalcon/zephir
$ cd zephir
$ ./install-json
$ zephir help
_____              __    _
/__  /  ___  ____  / /_  (_)____
/ /  / _ \/ __ \/ __ \/ / ___/
/ /__/  __/ /_/ / / / / / /
/____/\___/ .___/_/ /_/_/_/
      /_/
Zephir version 0.7.1b
</code></pre></li>
</ol>
<h2 id="初始化扩展"><a href="#初始化扩展" class="headerlink" title="初始化扩展"></a>初始化扩展</h2><pre><code>zephir init upfriend(upfriend扩展名)
upfriend/
   ext/
   upfriend/
</code></pre><h2 id="编写扩展"><a href="#编写扩展" class="headerlink" title="编写扩展"></a>编写扩展</h2><p>在<code>upfriend/upfriend</code>目录中创建<code>filter.zep</code>文件</p>
<pre><code>namespace Upfriend;
class Filter
{
    /**     
    * Filters a string returning its alpha characters
    * @param string str     
    */
    public function alpha(string str)
    {
        char ch; string filtered = &quot;&quot;;

        for ch in str {
            if (ch &gt;= &#39;a&#39; &amp;&amp; ch &lt;= &#39;z&#39;) || (ch &gt;= &#39;A&#39; &amp;&amp; ch &lt;= &#39;Z&#39;)
            {
              let filtered .= ch;
            }
        }

        return filtered;
    }
    public static function hello()
    {
        echo &quot;hello zephir&quot;;
    }
}
</code></pre><h2 id="编译扩展"><a href="#编译扩展" class="headerlink" title="编译扩展"></a>编译扩展</h2><p><code>upfriend</code>根目录运行<code>zephir bulid</code>编译扩展</p>
<pre><code>$ zephir build     
Preparing for PHP compilation...
Preparing configuration file...
Compiling...
Installing...
Extension installed!
Add extension=utils.so to your php.ini
Don&#39;t forget to restart your web serverp
</code></pre><h2 id="使用扩展"><a href="#使用扩展" class="headerlink" title="使用扩展"></a>使用扩展</h2><p>编译成功，修改php.ini配置文件，添加一行</p>
<pre><code>extension=upfirend.so //upfriend.so与php.ini位于同一目录
php -m  //查看扩展是否安装成功
</code></pre><p>在php中使用这个类（扩展）的方法</p>
<pre><code>&lt;?php
    $filter = new Upfriend\Filter();
    echo $filter-&gt;alpha(&quot;01he#l.lo?/1&quot;); // prints hello
    Upfriend::hello() // prints hello zephir
</code></pre>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[数据库范式的思考]]></title>
      <url>http://upeng.github.io/2016/06/25/normal/</url>
      <content type="html"><![CDATA[<p>还记得第一本数据库教程《数据库系统概论》，从最初的范式讲起，只记得当时学的还模模糊糊，今天又看到类似的文章【从范式谈起-余晟】，粗略总结一下数据库范式，也是加深一下记忆吧！在这之前，说下我对关系型数据库管理系统RDBMS和NOSQL的理解：RDBMS主要突出其R(Relation)关系；而NoSQL如：常见的MC、Redis、MongoDB，支持key-value型等简单的数据类型，适用于大数据时代的很多场景，为开发提供了巨大便利；使用场景不同，各有千秋。<br><a id="more"></a><br>数据库范式，简单的说就是<strong>规范化，在不丢失数据的情况下，把表拆分成更小、冗余度更小的表，通过不同表之间的外键用链接join方式得到需求数据</strong>，范式NF可以粗略的理解为一张数据表的表结构所符合的某种设计标准的级别，数据库范式也分为1NF，2NF，3NF，BCNF，4NF，5NF。</p>
<p><strong>通常一张表满足了第三范式，就认为这个表是规范化的，增删改查不会出现异常。</strong></p>
<h3 id="第一范式：属性不可拆分"><a href="#第一范式：属性不可拆分" class="headerlink" title="第一范式：属性不可拆分"></a>第一范式：属性不可拆分</h3><p>即表中的任何列（属性）都应当是承载信息的最小单位，不可再分；<br><em>1NF是所有关系型数据库的最基本要求</em></p>
<h3 id="第二范式：主键必须最小"><a href="#第二范式：主键必须最小" class="headerlink" title="第二范式：主键必须最小"></a>第二范式：主键必须最小</h3><p>即：在满足第一范式的基础上，所有非主属性必须完全依赖于主键，而且不能依赖于主键的某个子集<br><img src="http://upeng.github.io/image/table1.png" alt="表格"><br>主键：（学号，课名），系主任通过学号就可以确定；上表不满足2NF</p>
<blockquote>
<p>如何判断是否符合2NF？</p>
</blockquote>
<ul>
<li>第一步：找出数据表中所有的码。</li>
<li>第二步：根据第一步所得到的码，找出所有的主属性。</li>
<li>第三步：数据表中，除去所有的主属性，剩下的就都是非主属性了。</li>
<li>第四步：查看是否存在非主属性对码的部分函数依赖。</li>
</ul>
<p><em>2NF在1NF的基础之上，消除了非主属性对于码的部分函数依赖</em></p>
<h3 id="第三范式（3NF）：主键必须直接依赖，不能传递依赖"><a href="#第三范式（3NF）：主键必须直接依赖，不能传递依赖" class="headerlink" title="第三范式（3NF）：主键必须直接依赖，不能传递依赖"></a>第三范式（3NF）：主键必须直接依赖，不能传递依赖</h3><p><img src="http://upeng.github.io/image/table2.png" alt="表格"><br>上图表1符合3NF<br>上图表2主键是学号，其他3个是非主属性，学号-&gt;系名，系名-&gt;系主任；存在传递依赖，故表2不满足3NF<br><em>3NF在2NF的基础之上，消除了非主属性对于码的传递函数依赖</em>。</p>
<p>更深入的理解：<a href="https://www.zhihu.com/question/24696366" target="_blank" rel="external">知乎</a> ，注：文中图片均来源于此</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[In Recent Days]]></title>
      <url>http://upeng.github.io/2016/06/17/thinking-1/</url>
      <content type="html"><![CDATA[<p>首先我想吐糟一下为知笔记；我写了这么久，保存一下就丢了？丢了？发现最近为知还炒鸡慢，算了还是用Evernote吧，相对而言更靠谱（虽然不支持markdown格式）<br><a id="more"></a><br>最近忙的没有规律，蓝v后台一直在处理零星bug，但是对蓝v心里不是很踏实；目前为知，对做蓝v后台有几点体会</p>
<ul>
<li>代码结构混乱，一个函数能有几百行，变量超级多，定位问题很困难；不同的页面使用同一个Controller和view，然后在里面做各种复杂的判断逻辑，代码辣眼睛！</li>
<li>代码逻辑混乱，队列夹杂着数据库、调前台、平台接口同步数据；接口调的特隐晦，后台自己操作数据库要先入队列，然后通过脚本读队列操作数据库；其实量真没那么大！</li>
<li>主观方面自己心不静，看到代码复杂心情就不好了，为了能够实现功能就又开始堆代码了，心情很复杂，想重构又无从下手！</li>
</ul>
<p>先说下自己这半个月来的学习进度；月初和校长约定要学习这些东西：</p>
<ul>
<li>mysql分库分表、主从复制同步；</li>
<li>laravel和EPF的比较</li>
<li>swoole的学习</li>
</ul>
<p>这个月已经过半，直到今天才写了这个文章，并不是说这半个月没有学，相反，几乎天天都在学，地铁上班和回去的路上，中午有时候午休前也会看；但是没有总结，没有思考，只是机械的装载知识，并没有多少收获；下午和校长谈话，校长给了很中肯的意见，说的很多地方都是我目前遇到的问题；总结这半个月，我也看了很多东西：laravel、vuejs、swoole。但都只是停留在表面，没有深入！我觉得校长说的很对，学习的时候要有个主线，沿着这个主线深入学习，遇到与主线相关的问题，就深入学习搞定它；与主线无关就先getpocket，或者收藏起来；等主线相关的内容都搞明白搞清楚后，再去搞定你pocket里的另一个主线。想想这半个月以来的学习状态，我不禁要批评自己了，偏离主线的方向有点远了！</p>
<p>今天看到了<a href="http://xiaolai.li" target="_blank" rel="external">xiaolai</a>老师的文章，他说道：真正的工程师，时间分配大抵上是这样的，80% 的时间用来思考，20% 的时间把思考结果用代码表达出来，这个思考的过程包括反复搜索(search 多了，就叫 research)，深入阅读各种文档（因为今天的工程师必定要用到大量别人写好的模块），还要思考自己写出来的代码如何写好文档才能让更多的人使用……而刚入门的时候，可能是反过来的，80%的时间用来写代码，没有思想可以表达，就去模仿表达别人的思想…… 但，一定要有 20% 的时间花在真正的思考上，像工程师一样思考。<strong>很多人进步慢的根本原因在于，每天都在“学”，但从来不花大块的时间深入“想”。而我这半个月就是这种状态：每天都在学，但是没有花时间思考！</strong></p>
<p>另外我在学习的时候有个问题，比如在学laravel和vuejs的时候，我总想把文档一次性看完，然后再去操作；其实这犯了学习大忌，就像xiaolai老师说的</p>
<blockquote>
<p>真实的学习过程是，学会一点最少必要知识（就是那些知道了之后就可以马上开始行动了的最少知识），然后就要马上开始实际操练…… 最终，大量的细节都是在实际操作过程中进一步学会的 —— 甚至，还有大量的细节如果不进行实际操练的话，就根本没机会学得到。</p>
</blockquote>
<p>这半个月来：</p>
<ul>
<li>思考的少，机械师忘脑袋装知识，只讲数量不讲质量，仅仅停留在表面</li>
<li>总结的少，有点偏离主线</li>
</ul>
<p>后面要做的就是纠正这些学习上的毛病，一专而多精，看一个文章就要认真看，看懂！不能匆匆而过，看完要思考是什么？为什么？怎么做的？再针对这类问题做个总结，如果自己遇到这类问题你该怎么做，ok，fighting!</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[standing on the shoulders of the giants]]></title>
      <url>http://upeng.github.io/2016/05/29/thinking/</url>
      <content type="html"><![CDATA[<blockquote>
<p>It’s never too <strong>late</strong> to learn !</p>
</blockquote>
<p>周末学习laravel，然后写了一篇关于laravel认证相关的学习总结。写完后google类似的文章，意外发现了一个同学（不认识），<a href="http://aevit.xyz/" target="_blank" rel="external">博主</a>华师的，通过他的文章找到了他的github，然后一发不可收拾。<br><a id="more"></a></p>
<p>果然github真是一个全球最大的男性交友社区^_^，你会无意间发现很多大牛，发现他们的作品，他们的学习经历，现在看来买个vpn还是很值得的，程序员真要远离百度搜索。看见这些同学的博客，心里有种说不出的感觉，他们有的还在学校，没有毕业，惭愧啊！！当年自己本科都干了什么啊，真是。。。。他们已经在github上公开了自己的很多项目，而且励志成为full stack！自己发布了一些视频，很精美，称他们为全栈工程师一点也不过分！</p>
<p>看他人的学习成长过程，感到很惭愧，也很懊悔，不过我相信一切都还来得及，我还年轻，嗯，我还很年轻！O(∩_∩)O~</p>
<p>从他们的文章或者视频中能发现很多<strong>便捷高效的工具</strong>和<strong>学习技巧</strong>，我还记得去年刚入职通关答辩的时候，几位leader给我说的话</p>
<ul>
<li>对新技术的敏感度-保持不断学习的态度&amp;多思考</li>
<li>代码的规范-逻辑检查&amp;多看代码</li>
</ul>
<p>多点一下你可能就会有意外的收获，没人会去手把手教你，而且你已经工作了一年了，如果总结的话，你有什么拿得出手的东西吗？~(&gt;_&lt;)~<br><!-- more --><br>今天早上看到晗姐（annalihan）的github时又无意间看到吴大师的github，大师写了一篇关于<a href="https://github.com/kumawu/blog/blob/master/%E5%A6%82%E4%BD%95%E5%81%9A%E5%A5%BD%E4%B8%80%E6%AC%A1%E6%99%8B%E5%8D%87%E5%87%86%E5%A4%87.md" target="_blank" rel="external">如何做好一次晋升准备</a>，瞬间无语了，努力吧少年！多积累多总结，多思考多发现！多看博客的<code>about</code>和<code>links</code>,你会发现很多彩蛋！</p>
<p>附上今天看到的一些大牛的（我眼中的）博客，后续抽时间多看看，fighting！</p>
<p><a href="http://aevit.xyz/2016/05/11/laravel-learning-2/#more" target="_blank" rel="external">学习啦</a><br><a href="http://zh.learnlayout.com/" target="_blank" rel="external">学习css布局-aevit文章中提到</a><br><a href="http://rrytip.github.io/" target="_blank" rel="external">博客不错-绝对干货</a><br><a href="http://www.miaoyueyue.com/archives/452.html" target="_blank" rel="external">在sublime中运行php</a></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[laravel学习笔记-用户认证]]></title>
      <url>http://upeng.github.io/2016/05/29/laravel-auth/</url>
      <content type="html"><![CDATA[<p>在平时开发过程中，用户登录注册是非常常见的场景，比如大部分后台管理、论坛等等；用户一般web访问流程从业务来说是这样的：<br><a id="more"></a></p>
<ol>
<li>注册</li>
<li>登录</li>
<li>进入首页</li>
<li>查看&amp;操作</li>
<li>退出登录</li>
</ol>
<p>很多时候为了实现1、2、5这部分功能，重复造了很多轮子。目前接触到的laravel，就避免了你重复造轮子的问题，下面结合自己在laravelacademy上学习的laravel认证这部分做一下记录和总结，我学习的思路大致上是先学会用，然后搞清楚原理；所以这篇blog先记录如何使用的，快速实现function，尽管写的很low，但是给自己看，最关键的是是一个温习的过程，fighting！</p>
<p>laravel认证【插件】做了什么？</p>
<ul>
<li>注册</li>
<li>登录</li>
<li>找回密码（这都帮忙做好了^_^）</li>
</ul>
<h3 id="数据库表准备"><a href="#数据库表准备" class="headerlink" title="数据库表准备"></a>数据库表准备</h3><p>注册其实是入库的过程，也就是说，你需要有一个表专门用以存储注册用户的信息，laravel的<code>migration</code>已经自动创建了<code>users</code>和<code>password_resets</code>建表语句，只需<code>php artisan migrate</code>即可创建2张表到数据库；况且Model层已经创建了<code>User.php</code><br><!-- more --></p>
<h3 id="路由设置"><a href="#路由设置" class="headerlink" title="路由设置"></a>路由设置</h3><pre><code>// 认证路由...
Route::get(&#39;auth/login&#39;, &#39;Auth\AuthController@getLogin&#39;);
Route::post(&#39;auth/login&#39;, &#39;Auth\AuthController@postLogin&#39;);
Route::get(&#39;auth/logout&#39;, &#39;Auth\AuthController@getLogout&#39;);

// 注册路由...
Route::get(&#39;auth/register&#39;, &#39;Auth\AuthController@getRegister&#39;);
Route::post(&#39;auth/register&#39;, &#39;Auth\AuthController@postRegister&#39;);

// 发送密码重置链接路由
Route::get(&#39;password/email&#39;, &#39;Auth\PasswordController@getEmail&#39;);
Route::post(&#39;password/email&#39;, &#39;Auth\PasswordController@postEmail&#39;);

// 密码重置路由
Route::get(&#39;password/reset/{token}&#39;, &#39;Auth\PasswordController@getReset&#39;);
Route::post(&#39;password/reset&#39;, &#39;Auth\PasswordController@postReset&#39;);

//成功认证后网站首页
Route::get(&#39;/&#39;, &#39;UserController@index&#39;);
</code></pre><p>你会发现<code>AuthController</code>没有<code>getLogin、postLogin</code>这些方法，这里需要说明一下<a href="http://php.net/manual/zh/language.oop5.traits.php" target="_blank" rel="external">trait</a>官方手册讲述的已经很清楚，顺便学习一下overtrue的这篇文章<a href="http://overtrue.me/articles/2016/04/about-php-trait.html" target="_blank" rel="external">我所理解的 PHP Trait</a>，所以如果想知道auth怎么操作数据库，又是如何加载视图，做何种跳转，深入trait层去看代码就好。</p>
<h3 id="控制器"><a href="#控制器" class="headerlink" title="控制器"></a>控制器</h3><p>laravel已经创建好了auth控制器，位于<code>Controllers/Auth/</code>下，而且中间件也都默认创建，基本的准备工作已经做足，只需你创建view，做一些简单的设置就可以使用了</p>
<h4 id="部分属性设置"><a href="#部分属性设置" class="headerlink" title="部分属性设置"></a>部分属性设置</h4><p>注册登录和密码重置的路由入口都在<code>Controllers/Auth/</code>下的<code>AuthController.php</code>和<code>PasswordController.php</code>,他们都是用了<code>trait</code><br>如果你在控制器中没有定义一些path，<code>trait</code>底层都会使用自己定义的默认值，如果你喜欢这个默认值，你就按照他来写你的<code>route.php</code>吧，这里我们可以自己定义，覆盖<code>trait</code>中的这部分属性；如果不知道属性名称，进入<code>trait</code>查看</p>
<blockquote>
<p>eg:<br>```<br>//in AuthController.php<br>protected $redirectPath = ‘/‘;  //登录或注册成功进入首页，默认是/home<br>protected $redirectAfterLogout = ‘/auth/login’; //退出登录后跳转到登录页</p>
</blockquote>
<p>//in PasswordController.php<br>protected $redirectPath = ‘/‘;  //密码重置成功后跳转<br>protected $subject = ‘点击此处重置密码：’;  //邮件内容</p>
<pre><code>


#### 发挥认证作用
比如在登录成功后跳转的路由&#39;/&#39;，我们在`UserController`中怎么确定用户是否登录，如果没有登录怎么让用户成功登录后callback到其输入的地址；**auth中间件开始发挥其作用了**，所谓中间件，从字面上理解就是在用户访问该页面前，先去执行中间件内容。其实在Auth中也使用了中间件guest;在UserController中加入下面这个构造函数，即可让用户先登录再访问。
</code></pre><p>&lt;?php</p>
<p>namespace App\Http\Controllers;</p>
<p>use Illuminate\Http\Request;</p>
<p>use App\Http\Requests;<br>use App\Http\Controllers\Controller;</p>
<p>class UserController extends Controller<br>{<br>    public function __construct()<br>    {<br>        $this-&gt;middleware(‘auth’);<br>    }<br>    /**</p>
<pre><code> * Display a listing of the resource.
 *
 * @return \Illuminate\Http\Response
 */
public function index(Request $request)
{
    $user = $request-&gt;user();
    echo $user[&#39;name&#39;] . &#39;登录成功！&#39;;
}
</code></pre><p>}</p>
<p>```</p>
<h3 id="发散"><a href="#发散" class="headerlink" title="发散"></a>发散</h3><h4 id="关于模板"><a href="#关于模板" class="headerlink" title="关于模板"></a>关于模板</h4><p>使用Form表单类，加上bootstrap打造属于自己的登录注册界面，模板文件名源于trait中定义的view，当然我们可以重写getLogin等方法，因为它们有的简单到只有一个<code>return veiw(*.blade.php)</code><br>主要创建以下模板：<a href="https://github.com/upeng/learn-laravel" target="_blank" rel="external">详细页面</a></p>
<ul>
<li>register.blade.php</li>
<li>login.blade.php</li>
<li>password.blade.php</li>
<li>reset.blade.php</li>
</ul>
<h4 id="关于设置session"><a href="#关于设置session" class="headerlink" title="关于设置session"></a>关于设置session</h4><p>我们常见的登录登出按钮，重置密码，获取当前登录用户的基本信息等等。开始搞起吧！<br>后面仔细研究源码，从根本上掌握auth认真的全流程。</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[几个值得深思的 PHP 面试问题]]></title>
      <url>http://upeng.github.io/2016/05/28/php-detail/</url>
      <content type="html"><![CDATA[<p>5个值得深思的 PHP 面试问题<br>文章所罗列的问题虽然看似简单，但是每个背后都涵盖了一个或几个大家容易忽视的基础知识点，希望能够帮助到你的面试和平时工作。<br><a id="more"></a><br><strong>Q1</strong></p>
<pre><code>$str1 = &#39;hello world&#39;;
$str2 = &#39;he&#39;;
if (strpos($str1, $str2))
{
    echo &quot;/&quot;&quot; . $str1 . &quot;/&quot; contains /&quot;&quot; . $str2 . &quot;/&quot;&quot;;
}
else
{
    echo &quot;/&quot;&quot; . $str1 . &quot;/&quot; does not contain /&quot;&quot; . $str2 . &quot;/&quot;&quot;;
}
</code></pre><p>正确运行的输出结果:<br><!-- more --><br><code>&quot;hello world&quot; does not contain &quot;he&quot;</code><br>strpos是返回字符串str2在str1的位置，没有找到则返回false；php是弱类型语言；然而实际上这次返回了0而在if语句中0也被当作false,所以我们需要对false做类型判断，正确的代码如下:</p>
<pre><code>$str1 = &#39;hello world&#39;;
$str2 = &#39;he&#39;;
if (strpos($str1, $str2) !== false)
{
    echo &quot;/&quot;&quot; . $str1 . &quot;/&quot; contains /&quot;&quot; . $str2 . &quot;/&quot;&quot;;
}
else
{
    echo &quot;/&quot;&quot; . $str1 . &quot;/&quot; does not contain /&quot;&quot; . $str2 . &quot;/&quot;&quot;;
}
</code></pre><p>需要注意的是我们使用了!==，在php 和 JS中= !相对== 更为严格需要要求数据类型一致。</p>
<p><strong>Q2</strong><br>下面的输出结果会是怎样？</p>
<pre><code>$x = 5;
echo $x;
echo &quot;&lt;br /&gt;&quot;;
echo $x+++$x++;
echo &quot;&lt;br /&gt;&quot;;
echo $x;
echo &quot;&lt;br /&gt;&quot;;
echo $x---$x--;
echo &quot;&lt;br /&gt;&quot;;
echo $x;
</code></pre><p>实际运行结果是</p>
<pre><code>5
11
7
1
5
</code></pre><p>关于 $x++ 和 $x–这个问题其实非常容易遇见，我们只需记住$x++使用最近的值，然后才自增。<br>运算符的优先级，++ 是明显高于 +，因此先执行++ 再执行 + 。关于运算符的优先级，有的时候我们真的可以通过括号来让我们的程序更让人直观的了解，毕竟代码不光是用于执行的，有的时候或许团队的可读性也是提高效率的一种。<br><strong>Q3</strong><br>关于变量的引用；</p>
<pre><code>$a = &#39;1&#39;;
$b = &amp;$a;
$b = &quot;2$b&quot;;
</code></pre><p>请问 $a 和 $b的值各位多少<br>部分第一时间会想到 $a=’1’  $b=’21’,仔细一看 $b=&amp;$a,这里$b是变量$a的引用而不是直接 赋值。</p>
<p><strong>Q4</strong><br>下面是true还是false</p>
<pre><code>var_dump(0123 == 123)；
var_dump(&#39;0123&#39; == 123);
var_dump(&#39;0123&#39; == 123);
</code></pre><p>var_dump(0123 == 123);// false,PHP会默认把0123当作8进制来处理，实际转化为10进制就是83，显然这不是相等的。<br>var_dump(’0123′ == 123);// true这里php会非常有趣的将’0123′转换成一个数字而且默认去掉了前面的0也就是123==123<br>var_dump(’0123′ === 123);// false很显然上面的问题已经说过了数字和字符串类型不一致。</p>
<p><strong>Q5</strong><br>下面的代码有什么问题吗？输出会是什么，怎样修复它</p>
<pre><code>$referenceTable = array();
referenceTable[&#39;val1&#39;] = array(1, 2);
referenceTable[&#39;val2&#39;] = 3;
referenceTable[&#39;val3&#39;] = array(4, 5);

$testArray = array();

$testArray = array_merge($testArray, $referenceTable[&#39;val1&#39;]);
var_dump($testArray);
$testArray = array_merge($testArray, $referenceTable[&#39;val2&#39;]);
var_dump($testArray);
$testArray = array_merge($testArray, $referenceTable[&#39;val3&#39;]);
var_dump($testArray);
</code></pre><p>实际输出如下：</p>
<pre><code>array(2) {[0] =&gt; int(1) [1] =&gt; int(2)}
NULL
NULL
</code></pre><p>运行的时候你或许还能看到下面的警告</p>
<pre><code>warning: array_merge() : Argument #2 is not an array
warning: array_merge() : Argument #1 is not an array
</code></pre><p>array_merge需要传入的参数都是数组，如果不是，则会返回null。 你可以这样修改</p>
<pre><code>$testArray = array_merge($testArray, (array)$referenceTable[&#39;val1&#39;]);
var_dump($testArray);
$testArray = array_merge($testArray, (array)$referenceTable[&#39;val2&#39;]);
var_dump($testArray);
$testArray = array_merge($testArray, (array)$referenceTable[&#39;val3&#39;]);
var_dump($testArray);
</code></pre>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[laravel migration]]></title>
      <url>http://upeng.github.io/2016/05/07/laravel-migration/</url>
      <content type="html"><![CDATA[<h2 id="laravel-migration工具"><a href="#laravel-migration工具" class="headerlink" title="laravel migration工具"></a>laravel migration工具</h2><h3 id="新建表articles步骤"><a href="#新建表articles步骤" class="headerlink" title="新建表articles步骤"></a>新建表<code>articles</code>步骤</h3><ul>
<li>migrations下生成<code>2015_12_13_104637_create_articles_table.php</code>文件<br><code>php artisan make:migration create_articles_table --create=articles</code></li>
<li>修改生成的文件<code>2015_12_13_104637_create_articles_table.php</code></li>
</ul>
<pre><code>Schema::create(&#39;articles&#39;, function (Blueprint $table) {
    $table-&gt;increments(&#39;id&#39;);
    $table-&gt;string(&#39;title&#39;);
    $table-&gt;text(&#39;content&#39;);
    $table-&gt;timestamp(&#39;published_at&#39;);
    $table-&gt;timestamps();
});
</code></pre><ul>
<li>保存文件，执行下面命令，数据库中生成articls表<br><code>php artisan migrate</code></li>
</ul>
<h3 id="删除刚才创建的table"><a href="#删除刚才创建的table" class="headerlink" title="删除刚才创建的table"></a>删除刚才创建的table</h3><pre><code>//回退操作
php artisan migrate:rollback
</code></pre><a id="more"></a>
<h3 id="增加表字段"><a href="#增加表字段" class="headerlink" title="增加表字段"></a>增加表字段</h3><ol>
<li><p>如果将table drop后在create中添加字段,会删除表中已有数据。</p>
</li>
<li><p>在migrations下新建文件<code>add_intro_into_articles</code><br>```<br>php artisan make:migration add_intro_into_articles –table=articles</p>
</li>
</ol>
<pre><code>
3. 修改add_intro_into_articles
</code></pre><p>public function up()<br>{<br>    Schema::table(‘articles’, function (Blueprint $table) {<br>        $table-&gt;string(‘intro’);<br>    });<br>}</p>
<p>public function down()<br>{<br>    Schema::table(‘articles’, function (Blueprint $table) {<br>        $table-&gt;dropColumn(‘intro’);<br>    });<br>}</p>
<pre><code>
* 注意关键字的不同，导致生成的文件中匿名函数书写的不同
</code></pre><p>–create=tablename<br>Schema::create(‘articles’,function(){});</p>
<p>–table=tablename<br>Schema::table(‘articles’, function(){});<br>```</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[laravel的blade视图模板引擎]]></title>
      <url>http://upeng.github.io/2016/04/29/laravel-blade/</url>
      <content type="html"><![CDATA[<h3 id="blade模板引擎使用"><a href="#blade模板引擎使用" class="headerlink" title="blade模板引擎使用"></a>blade模板引擎使用</h3><p>很多时候，我们的view模板有很多重复的地方，比如head、footer部分，当然可以定义一些共用的部分，置于common文件夹下，然后使用include将其引入进来。而blade有一套自己的策略</p>
<p>比如定义一个<code>common.blade.php</code>文件，如下：</p>
<pre><code>&lt;!DOCTYPE html&gt;
&lt;html&gt;
    &lt;head&gt;
        &lt;title&gt;Laravel&lt;/title&gt;
        &lt;link href=&quot;https://fonts.googleapis.com/css?family=Lato:100&quot; rel=&quot;stylesheet&quot; type=&quot;text/css&quot;&gt;
        &lt;style&gt;
            html, body {height: 100%;}
            body {margin: 0;width: 100%;display: table;font-weight: 100;font-family: &#39;Lato&#39;;}
            .container {text-align: center;display: table-cell;vertical-align: middle;
            }
            .content {text-align: center;display: inline-block;}
            .title {font-size: 96px;}
            .footer{color: green;font-size: 100px;}
        &lt;/style&gt;
    &lt;/head&gt;
    &lt;body&gt; 
        &lt;div class=&quot;container&quot;&gt;
            @yield(&#39;content&#39;)
            @yield(&#39;footer&#39;)
        &lt;/div&gt;
    &lt;/body&gt;
&lt;/html&gt;
</code></pre><a id="more"></a>
<ul>
<li>其中，<code>@yield(&#39;content&#39;)</code>和 <code>@yield(&#39;footer&#39;)</code>是根据不同模板的需求进行动态设置与填充的</li>
<li><code>welcome.blade.php</code>和<code>contact.blade.php</code>模板与<code>common.blade.php</code>样式相近，只是内容不同，或者后者内容更多。他们均可以继承<code>common.blade.php</code>的所有内容，需要content则可以自定义显示，需要footer可以自定义显示footer</li>
<li>示例如下</li>
</ul>
<pre><code># welcome.blade.php
@extends(&#39;common&#39;)      //继承common.blade.php
@section(&#39;content&#39;)     //开始
    &lt;div class=&quot;content&quot;&gt;
        &lt;div class=&quot;title&quot;&gt;Laravel 5 -welcome&lt;/div&gt;
    &lt;/div&gt;
@stop                   //结束


# contact.blade.php
@extends(&#39;app&#39;)
@section(&#39;content&#39;) 
    &lt;div class=&quot;content&quot;&gt;
        &lt;div class=&quot;title&quot;&gt;contact 825415473@qq.com&lt;/div&gt;
    &lt;/div&gt;
@stop
@section(&#39;footer&#39;)
    &lt;div class=&quot;footer&quot;&gt;
        Laravel 5
    &lt;/div&gt;
@stop
</code></pre><h3 id="blade中的条件判断和循环操作"><a href="#blade中的条件判断和循环操作" class="headerlink" title="blade中的条件判断和循环操作"></a>blade中的条件判断和循环操作</h3><pre><code># 条件判断和循环操作，通用方法是在&lt;?php **** ?&gt;
# blade有自己的语法
@extends(&#39;app&#39;)
@section(&#39;content&#39;)

@if($condiction == &#39;bool&#39;)
    &lt;h1&gt;{!!$name!!}&lt;/h1&gt;
@else
    &lt;h1&gt;{{$time}}&lt;/h1&gt;
@endif

&lt;h2&gt;
    @foreach($arr as $key =&gt; $value)
        {{$value}}
    @endforeach
&lt;/h2&gt;

@stop
</code></pre>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[mysql数据库之索引优化]]></title>
      <url>http://upeng.github.io/2016/04/27/mysqlperformance03/</url>
      <content type="html"><![CDATA[<h2 id="如何选择合适的列建立索引"><a href="#如何选择合适的列建立索引" class="headerlink" title="如何选择合适的列建立索引?"></a>如何选择合适的列建立索引?</h2><ol>
<li>在where从句、group by从句、order by从句、on从句中出现的列</li>
<li>索引字段越小越好</li>
<li>离散度大的列放到联合索引的前面</li>
</ol>
<blockquote>
<p>理解离散度</p>
<pre><code>SELECT * FROM payment where staff_id = 2 AND customer_id = 584;
</code></pre><p>是index(staff_id, customer_id)好还是index(customer_id, staff_id)好？</p>
</blockquote>
<p><strong>备注</strong>：比较staff_id和customer_id两者的离散度，那个离散度高，那个列就放置到联合索引的前面<br><a id="more"></a></p>
<blockquote>
<p>计算离散度：</p>
</blockquote>
<pre><code>SELECT count(distinct staff_id), count(distinct customer_id) FROM payment;
</code></pre><h2 id="索引优化SQL的方法"><a href="#索引优化SQL的方法" class="headerlink" title="索引优化SQL的方法"></a>索引优化SQL的方法</h2><h3 id="索引的维护及优化—重复及冗余索引"><a href="#索引的维护及优化—重复及冗余索引" class="headerlink" title="索引的维护及优化—重复及冗余索引"></a>索引的维护及优化—重复及冗余索引</h3><blockquote>
<p>重复索引</p>
</blockquote>
<p>重复索引是指相同的列以相同的顺序建立同类型的索引，如下表中primary key 和ID列上的索引就是重复索引</p>
<pre><code>create table test(
    id int not null primary key,
    name varchar(10) not null,
    titile varchar(50) not nullm,
    unique(id)
)engine=innodb;
</code></pre><blockquote>
<p>冗余索引</p>
</blockquote>
<p>冗余索引是指多个索引的前缀列相同，或是在联合索引中包含了主键的索引，下面这个例子中key(name, id)就是一个冗余索引</p>
<pre><code>create table test(
    id int not null primary key,
    name varchar(10) not null,
    titile varchar(50) not nullm,
    key(name, id)
)engine=innodb;
</code></pre><blockquote>
<p>工具检查重复及冗余索引</p>
</blockquote>
<pre><code>pt-duplicate-key-checker -uroot  -p 123 -h 127.0.0.1
</code></pre><h3 id="索引的维护及优化—删除不用索引"><a href="#索引的维护及优化—删除不用索引" class="headerlink" title="索引的维护及优化—删除不用索引"></a>索引的维护及优化—删除不用索引</h3><p> 目前mysql中还没有记录索引的使用情况，但是在perconMySQL和MariaDB中可以通过INDEX_STATISTICS表来查看哪些索引未使用，但是在MySQL中目前只能通过慢查询日志配合pt-index-usage工具来进行索引使用情况的分析</p>
<pre><code>pt-index-usage -uroot -p123 mysql-slow.log
</code></pre>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[laravel初体验]]></title>
      <url>http://upeng.github.io/2016/04/25/laravel-primary/</url>
      <content type="html"><![CDATA[<h3 id="访问流程"><a href="#访问流程" class="headerlink" title="访问流程"></a>访问流程</h3><blockquote>
<p>路由文件routes.php</p>
</blockquote>
<pre><code>/*默认是以匿名函数的方式进行路由,/是当前域名下加载视图文件welcome.blade.php */

Route::get(&#39;/&#39;, function () {
    return view(&#39;welcome&#39;);
});
Route::get(&#39;/about&#39;, function () {
    return view(&#39;about&#39;);
});
Route::get(&#39;/blog&#39;, function () {
    return view(&#39;blog.content&#39;);  //其中的“.”可以用“/”代替
});

//如果使用非匿名函数的方式

Route::get(&#39;/&#39;, &#39;UserController@index&#39;);
Route::get(&#39;/about&#39;, &#39;UserController@about&#39;);
Route::get(&#39;/blog&#39;, &#39;UserController@blog&#39;);
</code></pre><h3 id="创建控制器"><a href="#创建控制器" class="headerlink" title="创建控制器"></a>创建控制器</h3><blockquote>
<ol>
<li>可以手动创建Controller，也可以使用命令行创建控制器<code>php artisan</code>工具</li>
<li>参数 –plain使用则不会生成一些默认的方法如index，create等</li>
</ol>
</blockquote>
<pre><code>php artisan make:controller UserController
php artisan make:controller TestController --plain
</code></pre><h3 id="视图的加载"><a href="#视图的加载" class="headerlink" title="视图的加载"></a>视图的加载</h3><blockquote>
<p>加载视图的方法</p>
</blockquote>
<pre><code>return view(&#39;welcome&#39;);
return view(&#39;blog/title&#39;);
return view(&#39;blog.content&#39;);
</code></pre><blockquote>
<p>怎么向blade视图传递变量</p>
</blockquote>
<pre><code>$name = &#39;&lt;p style=&quot;color:red&quot;&gt;upfriend&lt;/p&gt;&#39;;
$time = date(&#39;Y-m-d&#39;, time());
$arr = array(&#39;first&#39; =&gt; &#39;jerry&#39;, &#39;last&#39; =&gt; &#39;bool&#39;);

//传递变量-通用方式传递给$data数组
$data[&#39;name&#39;] = $name;
$data[&#39;time&#39;] = $time; 
return view(&#39;blog.content&#39;, $data);

//Laravel链式操作方式 
return view(&#39;blog.content&#39;)-&gt;(&#39;name&#39; =&gt; $name);  //传递一个变量
return view(&#39;blog.content&#39;)-&gt;with(array(&#39;name&#39; =&gt; $name, &#39;time&#39; =&gt; $time));  //传递数组变量

//compact方法
return view(&#39;blog.content&#39;, compact(&#39;name&#39;, &#39;time&#39;, &#39;arr&#39;));  //去掉$的变量名
</code></pre><blockquote>
<p>如何正确输出变量的值</p>
</blockquote>
<pre><code>//原生PHP输出数据方式，默认会对变量中的字符会进行转义（xss?）
&lt;h1&gt;&lt;?=name?&gt;&lt;/h1&gt;   

//Laravel的blade模板引擎渲染方式，两个大括号{{}}表示转义后输出
&lt;h1&gt;{{name}}&lt;/h1&gt;  //浏览器输出&lt;p style=&quot;color:red&quot;&gt;upfriend&lt;/p&gt;

&lt;h1&gt;{!!name!!}&lt;/h1&gt;  //会见变量作为html,输出红色字体的upfriend
</code></pre>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[mysql之sql优化-慢查询日志开启和查看（1）]]></title>
      <url>http://upeng.github.io/2016/04/24/mysqlperformance01/</url>
      <content type="html"><![CDATA[<p>如何发现有问题的SQL？使用慢查询日志【slow_query_log】对有效率问题的sql进行监控</p>
<h3 id="查看慢查询日志开启状态"><a href="#查看慢查询日志开启状态" class="headerlink" title="查看慢查询日志开启状态"></a>查看慢查询日志开启状态</h3><pre><code>show variable  s like &#39;slow_query_log&#39;
</code></pre><p><img src="https://raw.githubusercontent.com/upeng/upeng.github.io/master/image/6a0acb38-fbe0-42e6-b9ca-f05955ca73e8.png" alt=""></p>
<h3 id="记录未使用索引的查询"><a href="#记录未使用索引的查询" class="headerlink" title="记录未使用索引的查询"></a>记录未使用索引的查询</h3><p>未使用索引的查询默认是关闭的，如下</p>
<pre><code>show variables like &#39;log_queries_not_using_indexes&#39;;
</code></pre><p><img src="https://raw.githubusercontent.com/upeng/upeng.github.io/master/image/2655ccd1-69ab-4c54-b7bf-50b01b2b45e5.png" alt=""><br>需要打开未使用索引的查询，即未使用索引的查询会记录在案</p>
<pre><code>set global log_queries_not_using_indexes=on
</code></pre><a id="more"></a>
<h3 id="慢查询时间设置"><a href="#慢查询时间设置" class="headerlink" title="慢查询时间设置"></a>慢查询时间设置</h3><p> 查询时间超过多长时间的查询记录会记录到日志中； 默认为慢查询的时间10秒，即 超过10s的查询会记录到慢查询日志中<br><img src="https://raw.githubusercontent.com/upeng/upeng.github.io/master/image/d99d63ee-d0e2-4bfb-8230-984d884b1528.png" alt=""> </p>
<pre><code>set global long_query_time=0;
</code></pre><p>a litter bug i think：在本窗口查询时发现慢查询时间还是10；但是重新打开一个窗口查询结果才是0<br>这样设置的目的是：不管什么查询，都会记录到慢查询日志中</p>
<h3 id="打开慢查询日志"><a href="#打开慢查询日志" class="headerlink" title="打开慢查询日志"></a>打开慢查询日志</h3><p>设置完上述两项后，我们再打开慢查询日志</p>
<pre><code>set global slow_query_log=on;
</code></pre><p>输入两个查询语句</p>
<pre><code>select * from actor limit 10;
select * from actor where first_name like &#39;%A%&#39;;
</code></pre><h3 id="打开日志文件"><a href="#打开日志文件" class="headerlink" title="打开日志文件"></a>打开日志文件</h3><p>日志文件位置，查看变量slow_query_log_file的值</p>
<pre><code>show variables like &#39;slow_query_log_file&#39;;
</code></pre><p>当然，我们也可以手动设置慢查询日志的存放路径及文件名</p>
<pre><code>set global slow_query_log_file = ****
</code></pre><h3 id="慢查询日志所包含的内容"><a href="#慢查询日志所包含的内容" class="headerlink" title="慢查询日志所包含的内容"></a>慢查询日志所包含的内容</h3><p><img src="https://raw.githubusercontent.com/upeng/upeng.github.io/master/image/e3f793a8-8b50-464c-b52b-5ffbc78ab187.png" alt=""> </p>
<ol>
<li>执行sql的主机信息<br><code># User@Host: root[root] @ localhost [127.0.0.1]</code></li>
<li>sql的执行信息<br><code># Query_time: 0.001008  Lock_time: 0.000000 Rows_sent: 104  Rows_examined: 200</code></li>
<li>sql执行时刻<br><code>SET timestamp=1461435221;</code></li>
<li>sql内容<br><code>select * from actor where first_name like &#39;%A%&#39;;</code></li>
</ol>
<h3 id="慢查询日志的分析工具"><a href="#慢查询日志的分析工具" class="headerlink" title="慢查询日志的分析工具"></a>慢查询日志的分析工具</h3><p>mysqldumpslow输出</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[tmux linux下的终端复用工具]]></title>
      <url>http://upeng.github.io/2016/04/23/tmux%E5%B7%A5%E5%85%B7/</url>
      <content type="html"><![CDATA[<h2 id="tmux常用命令"><a href="#tmux常用命令" class="headerlink" title="tmux常用命令"></a>tmux常用命令</h2><p><code>tmux</code>打开tmux服务，列出几个最常用的命令<br>①：ctrl+B ②：字符<br>如：</p>
<pre><code>ctrl+B % 纵向切屏，将屏幕切成左右两半

ctrl+B &quot; 单个双引号，水平切割屏幕上下两半

ctrl+B x 确定关掉当前面板（回话）吗？ &#39;y&#39; -&gt;删除当前会话

ctrl+B t 显示当前时间

ctrl+B ↑ ↓ → ← 会话之间跳转
</code></pre><p>更多命令参考<br><a href="http://kumu-linux.github.io/blog/2013/08/06/tmux/" target="_blank" rel="external">http://kumu-linux.github.io/blog/2013/08/06/tmux/</a></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[mysql添加用户管理员及授权]]></title>
      <url>http://upeng.github.io/2016/04/10/mysql-grant/</url>
      <content type="html"><![CDATA[<h2 id="mysql添加用户管理员及授权"><a href="#mysql添加用户管理员及授权" class="headerlink" title="mysql添加用户管理员及授权"></a>mysql添加用户管理员及授权</h2><p>比如添加test用户，grant 高级 DBA 管理 MySQL 中所有数据库的权限</p>
<h3 id="step1-创建用户"><a href="#step1-创建用户" class="headerlink" title="step1 创建用户"></a>step1 创建用户</h3><pre><code>insert into mysql.user(Host,User,Password) values(&quot;%&quot;,&quot;test&quot;,password(&quot;123qwe&quot;));
insert into mysql.user(Host,User,Password) values(&quot;localhost&quot;,&quot;test&quot;,password(&quot;123qwe&quot;));
注：%和localhost的区别
此处的&quot;localhost&quot;，是指该用户只能在本地登录，不能在另外一台机器上远程登录。如果想远程登录的话，将&quot;localhost&quot;改为&quot;%&quot;，表示在任何一台电脑上都可以登录。也可以指定某台机器可以远程登录。
</code></pre><h3 id="step2-为用户授权"><a href="#step2-为用户授权" class="headerlink" title="step2 为用户授权"></a>step2 为用户授权</h3><pre><code>授权格式：grant 权限 on 数据库.* to 用户名@登录主机 identified by &quot;密码&quot;;　
grant all privileges on testDB.testTable to test@localhost identified by &#39;1234&#39;;
grant all on *.* to test@&#39;%&#39;;  //全部数据库表的权限
</code></pre><h3 id="step3-刷新系统权限表"><a href="#step3-刷新系统权限表" class="headerlink" title="step3 刷新系统权限表"></a>step3 刷新系统权限表</h3><pre><code>flush privileges; //刷新系统权限表
</code></pre>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[一个脚本引发的思考]]></title>
      <url>http://upeng.github.io/2016/02/25/mysql-think/</url>
      <content type="html"><![CDATA[<h2 id="问题来源："><a href="#问题来源：" class="headerlink" title="问题来源："></a>问题来源：</h2><p>前段时间写了一个crontab脚本，每5分钟连接一次数据库，遍历库中待处理的任务，一旦发现待处理(status=0)的任务,则将该任务状态置为处理中(status=1)，然后开始执行这个任务（dealing process），执行完成status=2</p>
<blockquote>
<p>最初我是一次性将所有任务设为处理中，然后一个一个串行处理，这样效率极低，一个任务可能要1个小时，其他的只能排队等。</p>
</blockquote>
<a id="more"></a>
<p><strong>Q1:</strong>如果一次性插入多个任务到任务表（比如5个,初始都是status=0），当cron执行第三条的时候，5分钟到了，然后开启第二个cron进程，继续扫描任务表，发现有2个待处理，于是去处理。。。可是，这个时候，如果第一个进程刚刚执行完第3个任务，开始执行第4个任务（也就是第二个进程的第1个任务），但是总会有一个进程会拿到这个任务，另一个进程会出现执行失败的情况</p>
<p><strong>原因如下：</strong></p>
<p>先遍历数据库，将待处理任务取出<br><em>第一个进程，将5个任务存入数组</em><br>$firstTask = array(1,2,3,4,5)<br><em>5min后，第二个进程，将2个任务存入数组</em><br>$secondTask = array(4,5)<br>如果第4个任务被第一个进程执行，但是此时4、5任务均位于第二个进程待处理范围，会继续run，这时候update任务表时，返回false;因为第4个任务已经被第一个进程处理了（已经update了）！<br>当然，这是没有问题的，最终5个任务都会被处理掉，不管他们是那个进程处理的，但是过程中，会出现返回false，伴随更新数据库失败的发生，为了避免这一个问题的发生，在update任务表之前，要先查询任务的状态，如果处理中(status=1)，OK，跳出此次循环，continue。。。这样的话，可以充分利用<strong>多进程并行处理多任务</strong>。</p>
<p><strong>Q2:</strong>如果一个任务处理时间较长，当处理完成，再次更新任务状态时失败，因为数据库已经失联了！对，断开连接，为什么？因为如果长时间链接数据库，而又没有操作，相当占用服务资源，我们都知道，数据库的链接数量是有限的，如果一直占着资源不放，其他作业就无法进行。就像一句俗语你懂的！</p>
<p>所有数据库一般都设置了连接超时时间，比如30s自动断开连接，再次使用只能重新连接<br><strong>查看mysql当前连接数</strong><br><code>show processlist;</code><br><img src="http://upfriend-blog.stor.sinaapp.com/bucket%2F2016011202.jpg" alt="请输入图片描述"><br><strong>查看mysql超时时间设置</strong><br><code>show variables like &#39;%timeout%&#39;;</code><br><img src="http://upfriend-blog.stor.sinaapp.com/bucket%2F20160112002223.jpg" alt="请输入图片描述"></p>
<p>所以为了保证任务顺利完成，可以在每个sql操作前进行mysql的connection连接（基于单例模式，如果已经连接，则不需再连接）</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[Linux常用命令-awk]]></title>
      <url>http://upeng.github.io/2016/01/31/linux-awk/</url>
      <content type="html"><![CDATA[<p>详细的awk使用总结翻阅为知笔记</p>
<h2 id="awk"><a href="#awk" class="headerlink" title="awk"></a>awk</h2><p>awk中 !a[$0]++ 与 !a[$1…]++<br>功能：</p>
<blockquote>
<p>去重</p>
<ol>
<li>a[$0]是未定义的，即undef</li>
<li>!优先级高于++</li>
</ol>
</blockquote>
<p>1.!a[$0]++<br>!a[$0]++可以去除一行中重复的记录，以记录作为数组（可以将a理解为数组）索引；基于上面两点说明，这个不难理解。例子如下<br><img src="http://upfriend-blog.stor.sinaapp.com/1.jpg" alt="请输入图片描述"></p>
<p>从上面例子可以看出，重复的记录被删除掉，只保留重复记录中第一次出现的记录</p>
<p>2.!a[$1]++ 或者 !a[$2]++等等<br>!a[$1]++可以根据记录中某个域删除这个域相同的记录，也可以根据这个域相同，执行另外一些相应的操作。例子如下;<br><img src="http://upfriend-blog.stor.sinaapp.com/2.jpg" alt="请输入图片描述"></p>
<p>从上面例子可以看出，第二个域相同的多条记录被删除，只保留重复记录中第一次出现的记录</p>
<h3 id="查看文件夹下的文件个数-当前目录的文件和子文件夹的文件数"><a href="#查看文件夹下的文件个数-当前目录的文件和子文件夹的文件数" class="headerlink" title="查看文件夹下的文件个数(当前目录的文件和子文件夹的文件数)"></a>查看文件夹下的文件个数(当前目录的文件和子文件夹的文件数)</h3><p> <code>find ./ -type f | wc -l</code></p>
<h2 id="wc"><a href="#wc" class="headerlink" title="wc"></a>wc</h2><p>功能：</p>
<blockquote>
<p>统计指定文本文件的行数、字数、字符数</p>
</blockquote>
<p>用法：</p>
<ol>
<li>wc file</li>
<li>wc -l file</li>
<li>wc -w file</li>
<li>wc -c file</li>
</ol>
<p>文章部分来源：<a href="http://blog.163.com/njut_wangjian/blog/static/16579642520141027103125556/" target="_blank" rel="external">http://blog.163.com/njut_wangjian/blog/static/16579642520141027103125556/</a></p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[linux常用命令（总是forget）]]></title>
      <url>http://upeng.github.io/2016/01/30/linux%E5%91%BD%E4%BB%A4/</url>
      <content type="html"><![CDATA[<h2 id="linux操作历史查询"><a href="#linux操作历史查询" class="headerlink" title="linux操作历史查询"></a>linux操作历史查询</h2><ul>
<li>我们知道上下键可以选择出输入的history历史;</li>
<li>如果输入命令很多，只记得命令的部分字符，可以使用<code>ctrl+R</code>，然后模糊匹配，输入部分字符串，就可以定位命令</li>
</ul>
<h2 id="linux-删除已输入的命令-常用"><a href="#linux-删除已输入的命令-常用" class="headerlink" title="linux 删除已输入的命令(常用)"></a>linux 删除已输入的命令(常用)</h2><p><code>ctr + w</code> 往回删除一个单词，光标放在最末尾<br><code>ctl + u</code> 删除光标以前的字符<br><code>ctl + k</code> 删除光标以后的字符<br><code>ctl + a</code> 移动光标至的字符头<br><code>ctl + e</code> 移动光标至的字符尾<br><code>ctl + l</code> 清屏</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[常用的bash命令快捷建]]></title>
      <url>http://upeng.github.io/2015/10/28/linux-bash/</url>
      <content type="html"><![CDATA[<p><code>ctrl</code>键组合<br><code>ctrl+a</code>:光标移到行首。<br><code>ctrl+b</code>:光标左移一个字母<br><code>ctrl+c</code>:杀死当前进程。<br><code>ctrl+d</code>:退出当前 Shell。<br><code>ctrl+e</code>:光标移到行尾。<br><code>ctrl+h</code>:删除光标前一个字符，同 backspace 键相同。<br><code>ctrl+k</code>:清除光标后至行尾的内容。<br><code>ctrl+l</code>:清屏，相当于clear。<br><code>ctrl+r</code>:搜索之前打过的命令。会有一个提示，根据你输入的关键字进行搜索bash的history<br><code>ctrl+u</code>: 清除光标前至行首间的所有内容。<br><code>ctrl+w</code>: 移除光标前的一个单词<br><code>ctrl+t</code>: 交换光标位置前的两个字符<br><code>ctrl+y</code>: 粘贴或者恢复上次的删除<br><code>ctrl+d</code>: 删除光标所在字母;注意和backspace以及ctrl+h的区别，这2个是删除光标前的字符<br><code>ctrl+f</code>: 光标右移<br><code>ctrl+z</code> : 把当前进程转到后台运行，使用’ fg ‘命令恢复。比如top -d1 然后ctrl+z ，到后台，然后fg,重新恢复<br>esc组合<br><code>esc+d</code>: 删除光标后的一个词<br><code>esc+f</code>: 往右跳一个词<br><code>esc+b</code>: 往左跳一个词<br><code>esc+t</code>: 交换光标位置前的两个单词。</p>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[PHP设计模式-单例模式]]></title>
      <url>http://upeng.github.io/2015/10/14/php-design-single/</url>
      <content type="html"><![CDATA[<h2 id="单例模式"><a href="#单例模式" class="headerlink" title="单例模式"></a>单例模式</h2><p><code>场景</code>:有多个类需要读取数据库，而数据库连接类只需要一个对象就够了。其他的类只需调用这个类的连接对象即可<br><code>问题</code>：如何获取两个严格相等（全等）的对象（===）<br><code>答案</code>：只有当两个对象是同一个对象的时候才是全等的！</p>
<a id="more"></a>
<pre><code>//1、用同一个类new两个对象
class Single{}
$s1 = new Single();
$s2 = new Single();
echo $s1 === $s2 ? &#39;是同一个对象&#39; : &#39;不是同一个对象&#39;;   //不是同一个对象，尽管他们内部方法变量完全相同

//2、不是同一个对象，主要是因为new了两次，所以下面要封锁new操作
class Single
{
    protected function __construct(){}
}

$s1 = new Single();  

//3、封锁了外部的new操作，同时需要在内部进行new操作

class Single
{
    public static function getIns()
    {
        return new self();
    } 
    protected function __construct(){}
}
$s1 = new Single();
$s2 = new Single();
echo $s1 === $s2 ? &#39;是同一个对象&#39; : &#39;不是同一个对象&#39;;
//不是同一个对象！因为内部new了两次！

//4、getIns先判断实例
class Single
{
    protected static $ins = null;
    public static function getIns()
    {
        if (self::$ins === null)
        {
            self::$ins = new self();
        }
        return self::$ins;
    } 
    protected function __construct(){}
}
$s1 = new Single();
$s2 = new Single();
echo $s1 === $s2 ? &#39;是同一个对象&#39; : &#39;不是同一个对象&#39;;//是同一个对象

 &gt; 注:**方法前加final，则方法不能被覆盖**，否则当有类继承它的时候会覆盖construct,又不是同一个对象了，**类前加final，则类不能被继承**

//5、禁止覆盖方法和克隆对象
class Single
{
    protected static $ins = null;
    public static function getIns()
    {
        if (self::$ins === null)
        {
            self::$ins = new self();
        }
        return self::$ins;
    } 
    //禁止覆盖方法
    final protected function __construct(){}
    //防止克隆
    final protected function __clone(){}
}

class dbSingle extends Single
{

}
$s1 = new dbSingle();
$s2 = new dbSingle();

$s1 = Single::getIns();
$s2 = clone $s1;
</code></pre>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[PHP设计模式-工厂模式]]></title>
      <url>http://upeng.github.io/2015/10/12/php-design-model/</url>
      <content type="html"><![CDATA[<h2 id="简单工厂模式"><a href="#简单工厂模式" class="headerlink" title="简单工厂模式"></a>简单工厂模式</h2><p>直接上代码，直观了解工厂模式！</p>
<pre><code>&lt;?php
//接口，是客户端和服务端共同遵守的开发规范
interface db
{
    public function conn(){};
}

//服务端
class Dbmysql implements db
{
    public function conn()
    {
        echo &#39;connected the mysql&#39;;
    }
}

class Dbsqlite implements db
{
    public function conn()
    {
        echo &#39;connected the sqlite&#39;;
    }
}

//客户端 
$mysql = new Dbmysql();
$mysql-&gt;conn();

$sqlite = new Dbsqlite();
$sqlite-&gt;conn();

//如果服务端不想让客户端知道自己内部类的实现细节，统一一个固定的入口，这样就是我们所说的
//使用方知道的越少越好！将上述两个类封装起来

//服务端，开发接口Dbconnect::type(@param)

class Dbconnect 
{
    public static function type($type)
    {
        if ($type == &#39;mysql&#39;)
        {
            return new Dbmysql();
        }
        else if ($type == &#39;sqlite&#39;)
        {
            return new Dbsqlite();
        }
        else
        {
            throw new Exception(&quot;Error type is not exist&quot;, 1);

        }
    }
}

//客户端
$mysql = Dbconnect::type(&#39;mysql&#39;);
$mysql-&gt;conn();

$sqlite = Dbconnect::type(&#39;sqlite&#39;);
$sqlite-&gt;conn();
</code></pre><p>在我们使用围脖的接口时，和这个有点类似，一个业务需求给你一个方法如showbatch(),friendship(),show()；当然他们是某个类的静态方法，我们不需要知道它是如何具体实现的，只要知道，当我们传递合适的参数，使用这个接口，就可以获得自己想要的数据即可</p>
<p><code>Q:现在如果需要连接oracle、PDO、SqlServer，怎么办？当然php可以直接修改服务端的代码，增加else if，但是对于java,c++而言，如果修改代码，还需要再次编译，这是非常耗时的!</code></p>
<blockquote>
<p><strong>在面向对象设计（OOD）设计法则中，重要的开闭原则：对于修改是封闭，对于扩展是开放的</strong></p>
</blockquote>
<h2 id="工厂方法"><a href="#工厂方法" class="headerlink" title="工厂方法"></a>工厂方法</h2><p><strong>注意开闭原则</strong><br>//<strong>服务端</strong></p>
<pre><code>interface Factory
{
    public function createDb(){};
}

class mysqlFactory implements Factory
{
    public function createDb()
    {
        return new Dbmysql();
    }
}

class sqliteFactory implements Factory
{
    public function createDb()
    {
        return new Dbsqlite();
    }
}

//**客户端**
//由于开放了两个接口，一个是创建数据库，一个是连接数据库

$fact = new mysqlFactory();
$db = $fact-&gt;createDb();
$db-&gt;conn();

$fact = new sqliteFactory();
$db-&gt;$fact-&gt;createDb();
$db-&gt;conn(); 

//现在如果新增了oracle数据库连接，就可以扩展oracle，不需要修改代码，扩展如下：

class Dboracle implements db()
{
    public function conn()
    {
        echo &#39;connected oracle database&#39;;
    }
}
class oracleFactory implements Factory
{
    public function createDb()
    {
        return new Dboracle();
    }
}


$fact = new oracleFactory();
$db = $fact-&gt;createDb();
$db-&gt;conn();
</code></pre>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[CI模型-视图-控制器 命名规范]]></title>
      <url>http://upeng.github.io/2015/08/13/php-ci1/</url>
      <content type="html"><![CDATA[<h3 id="controller："><a href="#controller：" class="headerlink" title="controller："></a>controller：</h3><pre><code>控制器类名必须以大写字母开头
         class  User  extends  CI_Controller{}          //建议写法
         class  BlogUser  extends  CI_Controller{}  //建议写法  
 以下这样写是无效的
         class  blog  extends  CI_Controller{}
         class  blogUser  extends  CI_Controller{}

 控制器类的方法没有要求

 控制器文件名也没有要求，但是建议全部小写，且注意文件名和类名同一个单词
 controller/blog.php 、controller/bloguser.php 
</code></pre><h3 id="model"><a href="#model" class="headerlink" title="model"></a>model</h3><pre><code>模型类名首字母也必须大写，其他小写
例如：
class  Blog_model  extends  CI_Model {    //class BlogModel extends  CI_Model也可
    function __construct(){
        parent::__construct();
    }
}

载入model以及使用model
$this-&gt;load-&gt;model(&#39;Blog_model&#39;);    //model类名  $this-&gt;load-&gt;model(&#39;BlogModel&#39;); 
$this-&gt;Blog_model-&gt;function();         //载入之后可直接通过类名访问其方法$this-&gt;BlogModel-&gt;function();

模型的文件名没有要求，但是建议全部小写,且注意文件名和类名同一个单词 
例如：model/blog_model.php   //       model/blogmodel.php
</code></pre><h3 id="view"><a href="#view" class="headerlink" title="view"></a>view</h3><pre><code>文件名是 views/viewname.php
载入视图 $this-&gt;load-&gt;view(&quot;viewname&quot;);
总之：文件名和载入一致即可，大小写没有特别要求
例如：文件名为views/viewName.php  载入时$this-&gt;load-&gt;view(&quot;viewName&quot;);

用子文件夹存储视图
views/user/bloguser.php   载入时$this-&gt;load-&gt;view(&quot;user/bloguser&quot;);



总结，对于模型和控制器，类名的首字母必须大写，其他字母无要求，可根据个人习惯
          文件名没有特别要求，建议首字母小写或全小写，名字与类名一致（除大小写可不一致）载入的模型和模型类名一致

          对于视图，文件名和载入时一致即可，载入时不带后缀 

  因为model和controller如果很多，载入model时不可能一个一个打开查看其类名，但是可以快速根据文件名确定其类名

  那就要求对文件的命名和类的命名要有一个一致的规范

我的习惯（保持）
  【1】controller,类名User或UserBlog，文件名user.php或userblog.php
  【2】model，类名UserModel或UserBlogModel ，文件名usermodel.php或userblogmodel.php
   载入方法$this-&gt;load-&gt;model(&quot;UserModel&quot;);
   用在构造方法里如下：
   class BlogModel  extends  CI_Controller{
       function __construct(){
            parent::__construct();
            $this-&gt;load-&gt;model(&#39;UserModel&#39;); 
        }
    }
  【3】view，文件名userblog.php 或user/blog.php  载入方法$this-&gt;load-&gt;view(&quot;userblog&quot;);或
    $this-&gt;load-&gt;view(&quot;user/blog&quot;);
</code></pre>]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[linux下导入、导出mysql数据库命令]]></title>
      <url>http://upeng.github.io/2015/08/03/mysql%E5%AF%BC%E5%85%A5%E5%AF%BC%E5%87%BA/</url>
      <content type="html"><![CDATA[<h2 id="一、导出数据库用mysqldump命令（注意mysql的安装路径，即此命令的路径）："><a href="#一、导出数据库用mysqldump命令（注意mysql的安装路径，即此命令的路径）：" class="headerlink" title="一、导出数据库用mysqldump命令（注意mysql的安装路径，即此命令的路径）："></a>一、导出数据库用mysqldump命令（注意mysql的安装路径，即此命令的路径）：</h2><ul>
<li><h3 id="导出数据和表结构："><a href="#导出数据和表结构：" class="headerlink" title="导出数据和表结构："></a>导出数据和表结构：</h3><p>mysqldump -u用户名 -p密码 数据库名 &gt; 数据库名.sql<br>eg /usr/local/mysql/bin/   mysqldump -uroot -p abc &gt; abc.sql<br>敲回车后会提示输入密码</p>
</li>
<li><h3 id="只导出表结构"><a href="#只导出表结构" class="headerlink" title="只导出表结构"></a>只导出表结构</h3><p>mysqldump -u用户名 -p密码 -d 数据库名 &gt; 数据库名.sql<br>eg /usr/local/mysql/bin/   mysqldump -uroot -p -d abc &gt; abc.sql</p>
</li>
</ul>
<p>注：/usr/local/mysql/bin/  —&gt;  mysql的data目录</p>
<h2 id="二、导入数据库"><a href="#二、导入数据库" class="headerlink" title="二、导入数据库"></a>二、导入数据库</h2><ol>
<li><h3 id="首先建空数据库"><a href="#首先建空数据库" class="headerlink" title="首先建空数据库"></a>首先建空数据库</h3><p>mysql&gt;create database abc;</p>
</li>
<li><h3 id="导入数据库"><a href="#导入数据库" class="headerlink" title="导入数据库"></a>导入数据库</h3><p>方法一：<br>（1）选择数据库<br>mysql&gt;use abc;<br>（2）设置数据库编码<br>mysql&gt;set names utf8;<br>（3）导入数据（注意sql文件的路径）<br>mysql&gt;source /home/abc/abc.sql;<br>方法二：<br>mysql -u用户名 -p密码 数据库名 &lt; 数据库名.sql<br>eg mysql -uabc_f -p abc &lt; abc.sql</p>
</li>
</ol>
<p>建议使用第二种方法导入。</p>
<h3 id="三、关于导入导出数据库需注意的地方（总是忘记！）"><a href="#三、关于导入导出数据库需注意的地方（总是忘记！）" class="headerlink" title="三、关于导入导出数据库需注意的地方（总是忘记！）"></a>三、关于导入导出数据库需注意的地方（总是忘记！）</h3><ul>
<li><p><code>而导入数据库和导出正好相反（废话），要先进入mysql，然后使用source命令</code></p>
</li>
<li><p><code>导出数据库是在linux命令行模式中，当前位于哪个目录就导出到哪里（途中可能需要输入user password）</code> </p>
</li>
</ul>
]]></content>
    </entry>
    
    <entry>
      <title><![CDATA[jquery中html()、text()、val()与js中的对应属性]]></title>
      <url>http://upeng.github.io/2015/07/15/html-js/</url>
      <content type="html"><![CDATA[<h3 id="html"><a href="#html" class="headerlink" title="html()"></a>html()</h3><p>类似于javascript的InnerHtml属性，可以用来读取或者设置某个元素中的HTML内容<br> 例如：Javascript代码  </p>
<pre><code>    &lt;p&gt;&lt;strong&gt;Hello world&lt;/strong&gt;&lt;/p&gt;  
     alert($(&#39;p&#39;).html());   
    返回： &lt;strong&gt;Hello world&lt;/strong&gt;
</code></pre><h3 id="text"><a href="#text" class="headerlink" title="text()"></a>text()</h3><p> 类似于javascript的InnerText属性，可以用来读取或者设置某个元素的文本内容<br>  例如：  Javascript代码 </p>
<pre><code>    &lt;p&gt;&lt;strong&gt;Hello world&lt;/strong&gt;&lt;/p&gt;   
      alert($(&#39;p&#39;).text());   
    返回：  Hello world
</code></pre><h3 id="val"><a href="#val" class="headerlink" title="val()"></a>val()</h3><p> 类似于javascript中的value属性，可以用来设置或者获取元素的值，无论元素是文本框，下拉列表还是单选框，如果元素时多选，则返回一个包含所有选择的值的数组。 </p>
]]></content>
    </entry>
    
  
  
</search>
